# GOAL — pigauto 0.11 pkgdown-011-repair (IMMUTABLE — re-read at the top of EVERY arc)

## Mission
Eliminate the named visual, contrast, table, and claim defects on the 0.11 user-facing pkgdown site. Written close report exists. PR #174 stays draft. CI green is not this gate.

## Headline
Repair the six G0-named defects; rebuild; see the rendered pages.

## Invariants
- Lane: **pkgdown-011-repair**. Lease paths: `pkgdown/`, `_pkgdown.yml`, `README.md`, `vignettes/`, `docs/dev-log/`, `LOOP/`, `.unlazy/`.
- Do **not** touch `R/`, `tests/`, `NEWS.md`, `DESCRIPTION`, `man/`.
- PR #175 PARK. No merge. No sentinel / Phase A/B numbers in docs.
- Dropbox `handover/2026-08-09-cursor` out of scope. Work only in `pigauto-011-ship`.
- No new `_brand.yml`. Florence/Tufte **gate**, not redesign.
- `r_cal = 0` remains a valid fallback. No new public capability claim.
- No push, no undraft, no merge of #174.
- AGENTS.md / CLAUDE.md out of scope this pass.
- `.unlazy/` stays gitignored.

## Authoritative WHAT
This file plus `LOOP/ultra-plan.md` (repair contract). Replaces the earlier visual-gate-only run.

## Approved defaults (do these)
1. Drop Step 7 GNN-beats-BM table in `getting-started.Rmd`; interpret via closed gate on high-signal morphometrics.
2. Tombstone `validation_suite.html` in place with withdrawn-page banner and noindex.
3. Body link contrast in `pkgdown/extra.css` to brand green `#2c5e4f` (7.4:1).
4. Replace raw `[!WARNING]` blockquotes in README and sidebar with Florence cream/amber alert banners.
5. Fix `gnn-architecture.Rmd`: remove BACE 42%→72% attribution; fix stale `phylopars()` text; format §5 table into scannable blocks.
6. AGENTS.md / CLAUDE.md out of scope.

## Definition of done
Named defects gone on the rebuilt site; `docs/dev-log/2026-08-27-pkgdown-visual-gate.md` written; ledger verified; #174 still draft.
