# Plan vs actual: freq vs BACE under Rubin's rules, steps 1 and 2

Reconciled by Melissa (Sonnet), 2026-09-24. Plan: `/Users/z3437171/.claude/plans/tidy-beaming-allen.md`.
Worktree: `pigauto-rubin-freq-bace` at `f5fad00` (parent `3f28e26`), branch `arc/rubin-freq-bace`.
Code review is out of scope for this reconciliation.

## Material deviations

| axis | planned | actual | tag | owner |
|---|---|---|---|---|
| evidence/verification | S4 smoke: one cell n=60, lambda 0.7, rho 0.5, M=20, no seed pinned | seed 1 failed deterministically (BACE "Mixed model equations singular"); smoke run and gated on seed 2, failure disclosed in after-task #9 and prerun-plan "Known risks" | adaptive | domain reviewer |
| evidence/verification | Context section: "BACE's continuous imputations are posterior means, with no residual drawn" (confirmed at stale `BACE/R/model_functions.R:646-649`) — the premise motivating S3's residual-draw arm | premise false for the installed build (2026-08-09 already draws a posterior predictive value + residual); true only for the stale in-tree clone (2026-04-01). `bace_resid` demoted from "the fix" to a negative control | drift | domain reviewer |
| evidence/verification | S3 was meant to test whether a residual draw fixes BACE's coverage | Meng's B2 finding: BACE as shipped is not proper MI regardless (every final run shares one deterministic anchor); toy coverage 0.90 vs 0.95 chained. No arm in this lane answers the lane's own question; a `bace_chain` arm is proposed and left open for Shinichi | drift | domain reviewer (finding), Ada (scope decision) |
| safety gates (D-139) | Lane plan's S5 row: grid gives "5 seeds = 720 fits", pre-run "~2 h Totoro" | Corrected to 240 fits (720 was an arithmetic slip), 253 core-hours, 2.5-3 h wall-clock, after finding the first budget reused v1 timings measured at 5 runs/20 final datasets as if they were 10/50 | drift | Ada |
| scope | Acceptance ledger names G-S1a/b for S1 and G-S4a/b for S4; Verification item 4 separately requires the R/BACE diff to be empty | G-S1c (estimand-recovery check) and G-S4c (the R/BACE diff, operationalized as a named gate) added to `.unlazy/rubin-freq-bace/gates/`; both strengthen, none narrow, the ledger | adaptive | Ada |
| evidence/verification | Plan defers to Meng's review to surface fix/defer items, no pre-committed scope for which non-blocking items get fixed now vs later | Beyond the 3 pre-run-blocking fixes (B1, bace-only selection, stale text), 5 more non-blocking findings (N2, N3, N4, N11, N14) were fixed in the same commit rather than deferred; 2 (N10, N12) were explicitly deferred to the campaign | adaptive | domain reviewer |
| handoff state | Closure section: "commit and push the branch" | Branch committed (2 commits) but not pushed; `git status -sb` shows `ahead 2` of `origin/arc/rubin-freq-bace` | drift | Rose |
| handoff state | Not addressed by this plan; standard lane discipline expects the lease released at close | Orchestrator reports the lease is still held; not independently verifiable from this worktree (no lease artifact found under its `.unlazy/`) | unclear | Rose |
| handoff state | Not addressed by this plan | `closeout.py new` wrote its template into the brain vault (`~/shinichi-brain` resolves `Github Local/Shinichi/`) instead of the worktree, touching the path the hub's brain-write boundary protects; moved out unchanged within minutes, nothing else in the vault touched | drift | Rose |
| evidence/verification | Not addressed by this plan | A broader closeout check reportedly fails only on an inherited, unrelated v1 ledger (`.unlazy/imputation-sim`), not on this lane's own gates (12/12 green); the after-task report does not overstate this, it only claims the 12/12 scoped to this lane | unclear | Rose |

Not material / confirmed matching, no row: model routing (R0 Explore/Sonnet was already "done" in the plan's own slice table before the 6-child fan-out budget line; S1-S3 general-purpose/Sonnet in parallel, S4/S5 orchestrator/Opus, V1 Haiku, V2 Meng/Opus, RC Melissa/Sonnet all match the plan's slice table exactly); the MUST-STOP gate (no Totoro/DRAC launch, no R/ or BACE/ edit — both held, confirmed by G-S4c and by the pre-run plan being written but not launched).

## Verdict

Routing, the parallel S1-S3 fan-out, and the hard stop before any Totoro launch or R/BACE edit all matched the plan; the deviations are concentrated in evidence (a load-bearing premise for the S3 arm turned out false, caught by review rather than by the plan) and in closeout mechanics (push, lease, and a stray vault write) rather than in scope creep or routing drift. Two items are unclear because this reconciliation could not independently verify them from the artifacts available in this worktree. Nothing here contradicts a public claim already made; the after-task and pre-run plan both state the corrected premise and defer BACE's design question to Shinichi rather than asserting a fix.
