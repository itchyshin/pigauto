# Plan vs actual: gnn-off (2026-09-18)

| axis | planned | actual | tag | owner |
|---|---|---|---|---|
| scope | Arc A only (S0 to S5, V1, V2, R1, C1); arc B locked but deferred to a fresh session, pre-run only under D-139 | Arc A delivered per contract; contract amended 5 times mid-run, all disclosed; R/check_pigauto.R edited though no slice's declared OWNS names it (still inside the R/ lease); script/campaign_gnn_off_cell.R (arc B scaffold) built and pushed inside this PR | adaptive, one drift | Ada, Rose |
| evidence/verification | ledger reverify, devtools test/check, AVONET300 smoke with output shown, fresh Fable adversarial pass | all four ran; ledger 10/10 gates MET; suite green after one wording-guard fix; check 0/0/0; smoke run and tabulated with a stated one-seed caveat; V2 Fable ran, APPROVE-WITH-FIXES, fixes applied and reverified | adaptive | domain reviewer |
| model routing | S1/S2/S4 Sonnet, S3/S5 reuse, V1 Haiku recon, V2 Fable, R1 Sonnet low, C1 reuse | S1, S2, S4, V1, V2 match; S0, S5, the V2-fix pass, gate-script fixes, the campaign runner and the Totoro pre-run step were done directly by the orchestrator (Opus) rather than dispatched or reused | adaptive | Ada |
| safety gates | D-43 panel fires at milestone = draft PR; MUST-STOP list bars merge, public claims, Totoro submit, baseline_full drift | no MUST-STOP item triggered; V2 found no baseline_full leakage; PR #180 is confirmed open (draft), so the D-43 milestone has in fact been reached though the panel has not fired yet | unclear | Rose |
| public claims | after-task report states only measured smoke results with regime caveats, no GNN-off vs GNN-on verdict | report correctly hedges ("one seed: a smoke, not evidence"; no comparative claim made); its own commit and file inventory omits the campaign-scaffold commit (7ec1bc1) that the PR body itself discloses | drift | Rose |
| handoff state | push feat/gnn-off, open a draft PR, never merge; continue here for arc A, fresh task for arc B | branch pushed, local matches origin; PR #180 open, draft, not merged; a LOOP/GOAL.md overwrite was caught and restored from HEAD before commit | adaptive | Rose |

## Drift items

- Campaign scaffold `script/campaign_gnn_off_cell.R` (commit 7ec1bc1) was built and shipped inside the arc-A PR, even though the plan's lane receipt calls for arc B to start as a fresh task, and the after-task report's own Goal section states the campaign "is a separate arc ... not this task." The PR description names the file; the after-task report does not. Owner: Rose, amend the after-task report's Files Touched and commit list before treating C1 as closed.
- R/check_pigauto.R was edited outside every slice's declared OWNS (S1 owns fit_pigauto/fit_baseline; S3 owns impute/multi_impute/multi_impute_trees/evaluate). It sits inside the general lane lease and is disclosed in the after-task report, so this reads closer to adaptive than drift, but no slice contract named it. Owner: Ada, tighten contract scoping next time.

## Unclear

- D-43 completion panel: the orchestrator's status note said the draft-PR milestone had not yet been reached, but PR #180 is confirmed open and draft as of this reconciliation. Either the panel is now due or the milestone needs restating. Owner: Rose.

## Sweep receipt

Phase 0.25 sweep in the plan is present and non-vacuous: concrete commands (git status/grep, brain search) with specific findings (129 commits behind, only `share_gnn = FALSE` hits, `force_gnn_off()` hack at ladder.R:188, the 2026-08-16 dev-log's unmeasured mechanisms) and a stated verdict, build-the-gap.

RECONCILE: 5 adaptive / 1 drift / 1 unclear
