# Phase Snapshot — pigauto

> **ONE entry. Replace it; never prepend.** Before adding a new entry, move the current one —
> **verbatim** — to `docs/dev-log/phase-snapshot-archive.md`. Repo state is truth; this block
> is a pointer, not a log.
>
> **Multi-lane:** do not replace this with a single-lane START HERE. Rehydrate from the
> coordination board’s Active Lane Split (every row).

- **2026-08-16 evening (earn-the-claims arc CLOSED)** · **START HERE: `docs/dev-log/handover/2026-08-16-claude-handover.md` (read its ⚡ UPDATE block first)** · `main` `1beda8e` — PRs #167 (silent-fallback honesty) + #168 (`conformal_method = "mondrian"`) merged, CI green, `--as-cran` 0E/0W/1N each · **four campaigns, 0 failures**: λ-attribution 1,920 (low-λ GNN gains = baseline misspecification, closure 1.00–1.17; F2 nonlinear lift SURVIVES λ-fitted baselines; `bayes` kills the ML boundary collapse) · mech-coverage 200 (exchangeability CONFIRMED) · mondrian verification 320 (repaired at n=1000; honest fallback below 19/stratum) · **first working BACE + Rphylopars/phylolm comparisons: raw Rphylopars beats pigauto continuous on AVONET300 (5 seeds) — the next arc's front-page question; mixed-type remains pigauto's win** · after-task `2026-08-16-earn-the-claims-arc.md` (§9 = ranked open items) · **open for Shinichi:** paper framing (evidence now assembled), continuous-gap diagnosis priority, index.lock `rm` + `git reset`
