url: https://itchyshin.github.io/pigauto

destination: docs

template:
  bootstrap: 5
  bootswatch: flatly    # gives a coloured (bg-primary) navbar, matching drmTMB/symbolizer
  bslib:
    primary: "#2c5e4f"   # brand green; matches prepR4pcm/_pkgdown.yml
    base_font: {google: "Inter"}
    heading_font: {google: "Inter"}
    code_font: {google: "JetBrains Mono"}

home:
  logo: man/figures/logo.png
  title: "pigauto: Phylogenetic Imputation via Graph AUTO-encoders"
  description: >
    pigauto imputes missing trait data for comparative analyses by
    combining three sources of information: phylogenetic similarity
    (closely related species share similar traits), cross-trait
    correlations (observed traits inform missing ones), and optional
    environmental covariates (climate, habitat, geography). It handles
    eight trait types in a single call: continuous measurements,
    counts, binary variables, unordered categories, ordered categories,
    bounded proportions, zero-inflated counts, and compositional
    (multi-proportion) data that sums to one across components.
    Uncertainty is described with conformal prediction intervals and an
    experimental Rubin's-rules multiple-imputation workflow for pooling
    fixed effects, with tree-uncertainty propagation via posterior tree
    samples. Variance components, random-effect predictions, and BLUPs are
    not pooled in version 0.10.0. Tested up to 10,000 species on a laptop.
  links:
    - text: Source code
      href: https://github.com/itchyshin/pigauto
    - text: Report a bug
      href: https://github.com/itchyshin/pigauto/issues

navbar:
  structure:
    left:  [intro, articles, methodology, reference, news]
    right: [search, github]
  components:
    intro:
      text: Get started
      href: articles/getting-started.html
    articles:
      text: Articles
      menu:
        - text: "Knitted vignettes"
        - text: "Get started"
          href: articles/getting-started.html
        - text: "Common pitfalls / FAQ"
          href: articles/common-pitfalls.html
        - text: "Mixed-type traits"
          href: articles/mixed-types.html
        - text: "Propagating tree uncertainty"
          href: articles/tree-uncertainty.html
        - text: "GNN architecture and the math behind pigauto"
          href: articles/gnn-architecture.html
        # Four static walk-through HTML pages (pigauto_intro,
        # pigauto_workflow_mixed, pigauto_walkthrough_covariates,
        # pigauto_walkthrough_multi_obs) were retired in 2026-05 to
        # dev/archive/ because their version badges, trait-type scope,
        # and BACE-comparison wording had gone stale.  Do not restore
        # them as live navbar entries.  If a fresh walk-through is
        # needed, write a new source-backed vignette under vignettes/
        # rather than reanimating the archived HTML.  Audit detail:
        # useful/pkgdown_page_rethink.md.
    methodology:
      text: Methodology
      menu:
        - text: "Per-trait benches"
        - text: "Continuous (BM / OU / regime / nonlinear)"
          href: dev/bench_continuous.html
        - text: "Binary (signal strength sweep)"
          href: dev/bench_binary.html
        - text: "Ordinal (level count sweep)"
          href: dev/bench_ordinal.html
        - text: "Count (Poisson / NegBin)"
          href: dev/bench_count.html
        - text: "Categorical (K-level sweep)"
          href: dev/bench_categorical.html
        - text: "Proportion (signal sweep)"
          href: dev/bench_proportion.html
        - text: "Zero-inflated counts (zero fraction sweep)"
          href: dev/bench_zi_count.html
        - text: "-------"
        - text: "Cross-dataset benches and simulations"
        - text: "AVONET missingness sweep (20 / 50 / 80 %)"
          href: dev/bench_avonet_missingness.html
        - text: "Missingness mechanisms (MCAR / MAR / MNAR)"
          href: dev/bench_missingness_mechanism.html
        - text: "Tree uncertainty (Rubin's rules, 10 posterior trees)"
          href: dev/bench_tree_uncertainty.html
        - text: "Delhey 5,809-species birds"
          href: dev/bench_delhey.html
        - text: "Multi-observation imputation (CTmax-like)"
          href: dev/bench_multi_obs.html
        - text: "Covariate effectiveness (simulation sweep)"
          href: dev/bench_covariate_sim.html
        # -------- Phase 2 placeholder --------
        # When Phase 2 ships, add an entry here for the math vignette:
        #   - text: "Mathematical & algorithmic description"
        #     href: articles/methodology.html
        #
        # -------- Historical benchmark evidence --------
        # The following pre-v0.10 snapshot pages remain under docs/dev/ as
        # historical benchmark evidence, but are intentionally absent from
        # the release navbar. Re-add them only after their script/make_*_html.R
        # driver has been rerun against the release code and the claims have
        # been reviewed:
        #   bench_multi_proportion, bench_scaling_v090, pantheria_summary,
        #   phase8_summary, bench_signal_sweep, bench_correlation_sweep,
        #   bench_evo_model_sweep, bench_clade_missingness,
        #   bench_bace_avonet_head_to_head, bench_pantheria_bace_head_to_head.

reference:
  - title: "One-call entry point"
    desc: >
      The main user-facing function. Runs the full pipeline
      (`preprocess_traits` → `build_phylo_graph` → `fit_baseline` →
      `fit_pigauto` → `predict` → `evaluate`) and returns a completed
      data frame.
    contents:
      - impute

  - title: "Experimental multiple imputation for fixed effects"
    desc: >
      Three functions that compose into the documented
      experimental multiple-imputation → regression → Rubin's-rules workflow
      (Rubin 1987; Nakagawa & Freckleton 2008, 2011). See
      `vignette("mixed-types")` and `vignette("tree-uncertainty")` for
      end-to-end examples. Version 0.10.0 pools fixed effects only; variance
      components, random-effect predictions, correlations, and BLUPs are not
      supported pooling targets.
    contents:
      - multi_impute
      - multi_impute_trees
      - with_imputations
      - pool_mi

  - title: "Pipeline — fine-grained control"
    desc: >
      Individual steps of the `impute()` pipeline, exposed for
      benchmarking and custom workflows.
    contents:
      - preprocess_traits
      - build_phylo_graph
      - make_missing_splits
      - mask_missing
      - fit_baseline
      - fit_pigauto
      - predict.pigauto_fit
      - evaluate
      - evaluate_imputation

  - title: "Active imputation (sampling-design guidance)"
    desc: >
      Per-candidate-observation expected uncertainty reduction across
      every currently-missing cell, using closed-form BM
      (Sherman-Morrison) variance reduction for continuous / count /
      ordinal / proportion / zi_count magnitude and label-propagation
      entropy reduction for binary / categorical / zi_count gate.
    contents:
      - suggest_next_observation

  - title: "Covariate data helpers"
    desc: >
      Optional helpers for assembling environmental covariate matrices
      from public data sources. Both are designed to plug into
      `impute(..., covariates = X)`.
    contents:
      - pull_gbif_centroids
      - pull_worldclim_per_species

  - title: "Benchmarks and cross-validation"
    contents:
      - simulate_benchmark
      - simulate_non_bm
      - cross_validate
      - compare_methods

  - title: "Reporting and plotting"
    contents:
      - pigauto_report
      - plot.pigauto_fit
      - plot.pigauto_pred
      - plot.pigauto_benchmark
      - plot_comparison
      - plot_history_gg
      - plot_uncertainty
      - summary.pigauto_fit
      - calibration_df
      - confusion_matrix

  - title: "I/O helpers"
    contents:
      - read_traits
      - read_tree
      - save_pigauto
      - load_pigauto

  - title: "Bundled data"
    contents:
      - avonet300
      - tree300
      - trees300
      - avonet_full
      - tree_full
      - delhey5809
      - tree_delhey
      - ctmax_sim
