# Phylogenetic-signal gate — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a per-trait phylogenetic-signal diagnostic (Pagel's λ) that forces the grand-mean corner `(0, 0, 1)` on the safety-floor simplex when `λ < threshold`, making "pigauto knows when phylogeny helps" an explicit, reportable property per trait.

**Architecture:** Compute λ via `phytools::phylosig()` at the top of `fit_pigauto()` (after `preprocess_traits()`, before `fit_baseline()`). Continuous traits use λ directly; binary/categorical/ordinal traits use λ on the z-scored latent liability. Traits with λ below threshold are written directly to `(r_cal_bm = 0, r_cal_gnn = 0, r_cal_mean = 1)` without entering the 3-way search. The fit object carries `phylo_signal_per_trait` + `phylo_gate_triggered`. `phytools` moves to Suggests.

**Tech Stack:** R ≥ 4.0, `testthat` 3rd edition, `roxygen2`, `devtools`, `phytools` (new Suggests), existing pigauto 0.9.1.9002.

**Working branch:** `feature/phylo-signal-gate` (stacked on `feature/safety-floor-mean-gate`, already carrying the spec commit `06de443`).

**Spec:** `specs/2026-04-23-phylo-signal-gate-design.md`.

---

## File structure

| Path | Purpose | Create/Modify |
|---|---|---|
| `R/phylo_signal.R` | new: `compute_phylo_signal_per_trait()`, `.phylo_signal_lambda_continuous()`, `.phylo_signal_lambda_binary()`, `.phylo_signal_lambda_categorical()` | create |
| `R/fit_pigauto.R` | call compute_phylo_signal_per_trait() before fit_baseline(); thread `phylo_signal_gate`, `phylo_signal_threshold`, `phylo_signal_method` args; force `r_cal_*` on gated traits; store the signal + gate slots on `pigauto_fit` | modify |
| `R/impute.R` | pass-through args + roxygen | modify |
| `R/print_pigauto_fit.R` | add "Phylogenetic signal" section to `print.pigauto_fit()` | modify (or create if absent) |
| `tests/testthat/test-phylo-signal-gate.R` | **new** smoke canary, ~8 test_that blocks | create |
| `DESCRIPTION` | add `phytools` to Suggests; bump to 0.9.1.9003 | modify |
| `NEWS.md` | new top section | modify |
| `NAMESPACE` | regenerated by `devtools::document()` | regenerate |

Total: 1 new R file, 3 modified R files, 1 new test file, 3 admin files.

---

## Task 1: Add `compute_phylo_signal_per_trait()` helper + unit tests

**Files:**
- Create: `R/phylo_signal.R`
- Create: `tests/testthat/test-phylo-signal-gate.R`

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test-phylo-signal-gate.R` with:

```r
# tests/testthat/test-phylo-signal-gate.R
# Smoke canary for phylogenetic-signal gate.
# See specs/2026-04-23-phylo-signal-gate-design.md.

test_that("compute_phylo_signal_per_trait returns lambda for continuous traits", {
  skip_if_not_installed("phytools")
  set.seed(2026L)
  n <- 200L
  tree <- ape::rcoal(n)
  # Strong-signal trait: simulate under BM
  t_strong <- ape::rTraitCont(tree, model = "BM", sigma = 1)
  # Weak-signal trait: white noise
  t_weak   <- stats::rnorm(n)
  traits <- data.frame(strong = t_strong, weak = t_weak,
                        row.names = tree$tip.label)
  data_obj <- pigauto::preprocess_traits(traits, tree)
  lambdas <- pigauto:::compute_phylo_signal_per_trait(
    data = data_obj, tree = tree, method = "lambda")
  expect_type(lambdas, "double")
  expect_named(lambdas, c("strong", "weak"))
  expect_gt(as.numeric(lambdas["strong"]), 0.7)   # strong BM signal
  expect_lt(as.numeric(lambdas["weak"]),   0.3)   # weak/no signal
})

test_that("compute_phylo_signal_per_trait handles constant column as NA", {
  skip_if_not_installed("phytools")
  set.seed(1L)
  n <- 50L
  tree <- ape::rcoal(n)
  traits <- data.frame(const = rep(3.14, n),
                        row.names = tree$tip.label)
  data_obj <- pigauto::preprocess_traits(traits, tree)
  lambdas <- pigauto:::compute_phylo_signal_per_trait(
    data = data_obj, tree = tree, method = "lambda")
  expect_true(is.na(as.numeric(lambdas["const"])))
})

test_that("compute_phylo_signal_per_trait returns NA below min_tips", {
  skip_if_not_installed("phytools")
  set.seed(1L)
  tree <- ape::rcoal(15L)  # below default min_tips = 20
  traits <- data.frame(x = stats::rnorm(15L),
                        row.names = tree$tip.label)
  data_obj <- pigauto::preprocess_traits(traits, tree)
  lambdas <- pigauto:::compute_phylo_signal_per_trait(
    data = data_obj, tree = tree, method = "lambda", min_tips = 20L)
  expect_true(is.na(as.numeric(lambdas["x"])))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::test(filter = "phylo-signal-gate")'`
Expected: 3 tests fail with `could not find function "compute_phylo_signal_per_trait"`.

- [ ] **Step 3: Write the implementation**

Create `R/phylo_signal.R`:

```r
# R/phylo_signal.R
# Phylogenetic-signal diagnostic: per-trait Pagel's λ used by
# fit_pigauto() to route weak-signal traits to the grand-mean corner
# of the safety-floor simplex. See
# specs/2026-04-23-phylo-signal-gate-design.md.

# Compute per-trait phylogenetic signal on training-observed cells.
#
# For continuous / count / ordinal / proportion / zi_mag latent columns,
# λ is computed on the z-scored latent values via phytools::phylosig().
# For binary / categorical latent columns, λ is computed on the
# estep-decoded posterior mean liability (still continuous on the
# liability scale).
#
# Returns a named numeric vector of length = length(data$trait_map),
# keyed by trait name. NA for traits where λ cannot be computed
# (too few tips, constant values, phytools missing).
#
# @param data     pigauto_data object from preprocess_traits()
# @param tree     phylo object matching data$species
# @param method   character, currently only "lambda" is implemented
# @param min_tips integer, skip traits with fewer than this many
#                 species in training observations (default 20)
# @return named numeric vector
#
# @noRd
compute_phylo_signal_per_trait <- function(data, tree,
                                            method = c("lambda", "blomberg_k"),
                                            min_tips = 20L) {
  method <- match.arg(method)
  if (!requireNamespace("phytools", quietly = TRUE)) {
    warning("phylo_signal_gate requires the 'phytools' package; ",
            "returning NA for all traits.", call. = FALSE)
    out <- rep(NA_real_, length(data$trait_map))
    names(out) <- vapply(data$trait_map, function(tm) tm$name,
                          character(1L))
    return(out)
  }
  tm_names <- vapply(data$trait_map, function(tm) tm$name, character(1L))
  out <- rep(NA_real_, length(tm_names))
  names(out) <- tm_names
  for (i in seq_along(data$trait_map)) {
    tm <- data$trait_map[[i]]
    lambda <- tryCatch(
      .phylo_signal_one_trait(tm, data, tree, method, min_tips),
      error = function(e) NA_real_)
    out[i] <- lambda
  }
  out
}

.phylo_signal_one_trait <- function(tm, data, tree, method, min_tips) {
  if (tm$type %in% c("continuous", "count", "ordinal",
                       "proportion", "zi_mag")) {
    .phylo_signal_lambda_continuous(tm, data, tree, method, min_tips)
  } else if (tm$type %in% c("binary", "categorical")) {
    .phylo_signal_lambda_discrete(tm, data, tree, method, min_tips)
  } else if (tm$type == "zi_count") {
    .phylo_signal_lambda_zi_count(tm, data, tree, method, min_tips)
  } else if (tm$type == "multi_proportion") {
    .phylo_signal_lambda_continuous(tm, data, tree, method, min_tips)
  } else {
    NA_real_
  }
}

.phylo_signal_lambda_continuous <- function(tm, data, tree, method, min_tips) {
  lc <- tm$latent_cols[1L]
  x  <- data$X_scaled[, lc]
  # Training-observed species
  obs <- !is.na(x)
  if (sum(obs) < min_tips) return(NA_real_)
  # Standard deviation guard
  if (stats::sd(x[obs], na.rm = TRUE) < 1e-10) return(NA_real_)
  # phylosig needs a named vector keyed by tip label
  sp_names <- if (!is.null(data$species)) data$species[obs] else
    rownames(data$X_scaled)[obs]
  vals <- x[obs]
  names(vals) <- sp_names
  # Restrict tree to these tips
  tr <- ape::keep.tip(tree, sp_names)
  vals <- vals[tr$tip.label]
  if (identical(method, "lambda")) {
    res <- phytools::phylosig(tr, vals, method = "lambda", test = FALSE)
    as.numeric(res$lambda)
  } else {
    res <- phytools::phylosig(tr, vals, method = "K", test = FALSE)
    as.numeric(res$K)
  }
}

.phylo_signal_lambda_discrete <- function(tm, data, tree, method, min_tips) {
  # For binary/categorical, use the z-scored latent column directly
  # as a continuous proxy. This is the simplest approach; more
  # sophisticated: run estep_liability_* first to get a posterior
  # mean liability. For the v0.9.1.9003 initial ship, we use the
  # direct z-scored proxy which is cheap and correlates well with
  # λ on the exact liability.
  .phylo_signal_lambda_continuous(tm, data, tree, method, min_tips)
}

.phylo_signal_lambda_zi_count <- function(tm, data, tree, method, min_tips) {
  # Coupled-gate convention: weak-signal iff BOTH latent columns are
  # weak. Return min of the two λs so the downstream threshold check
  # treats "either one weak" as "trait weak".
  lc_gate <- tm$latent_cols[1L]
  lc_mag  <- tm$latent_cols[2L]
  tm_gate <- list(latent_cols = lc_gate, type = "binary",
                   name = paste0(tm$name, "_gate"))
  tm_mag  <- list(latent_cols = lc_mag, type = "zi_mag",
                   name = paste0(tm$name, "_mag"))
  lam_gate <- .phylo_signal_lambda_discrete(tm_gate, data, tree, method, min_tips)
  lam_mag  <- .phylo_signal_lambda_continuous(tm_mag, data, tree, method, min_tips)
  if (is.na(lam_gate) && is.na(lam_mag)) return(NA_real_)
  if (is.na(lam_gate)) return(lam_mag)
  if (is.na(lam_mag))  return(lam_gate)
  max(lam_gate, lam_mag)   # use max so the gate triggers only when BOTH are weak
}
```

- [ ] **Step 4: Run test to verify pass**

Run: `cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::load_all(); devtools::test(filter = "phylo-signal-gate")'`
Expected: all 3 tests PASS.

- [ ] **Step 5: Commit**

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
git add R/phylo_signal.R tests/testthat/test-phylo-signal-gate.R
git commit -m "phylo-signal: add compute_phylo_signal_per_trait() helper + tests"
```

---

## Task 2: Add `phytools` to DESCRIPTION Suggests + guard

**Files:**
- Modify: `DESCRIPTION`

- [ ] **Step 1: Add phytools to Suggests**

Edit `DESCRIPTION`. Find the `Suggests:` block. Append `phytools` alphabetically:

```
Suggests:
    ...existing entries...
    phytools,
    ...remaining existing entries alphabetical...
```

If `Suggests:` doesn't exist, create it.

- [ ] **Step 2: Verify R CMD check still clean**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::check(args = "--no-tests", error_on = "error", quiet = TRUE)' 2>&1 | tail -n 5
```

Expected: 0 errors, 0 warnings, pre-existing notes unchanged.

- [ ] **Step 3: Commit**

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
git add DESCRIPTION
git commit -m "phylo-signal: add phytools to Suggests"
```

---

## Task 3: Thread `phylo_signal_gate` into `fit_pigauto()` + force `r_cal_*` on gated traits

**Files:**
- Modify: `R/fit_pigauto.R` (signature + body + return)

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-phylo-signal-gate.R`:

```r
test_that("fit_pigauto(phylo_signal_gate = TRUE) stores phylo_signal slots", {
  skip_if_not_installed("phytools")
  data("avonet300", package = "pigauto")
  data("tree300",   package = "pigauto")
  df <- avonet300
  if ("Species_Key" %in% colnames(df)) {
    rownames(df) <- df$Species_Key; df$Species_Key <- NULL
  }
  set.seed(2026L)
  df$Mass[sample(300, 30)] <- NA_real_
  res <- pigauto::impute(df, tree300,
                           phylo_signal_gate = TRUE,
                           phylo_signal_threshold = 0.2,
                           safety_floor = TRUE,
                           epochs = 50L, n_imputations = 1L,
                           verbose = FALSE, seed = 2026L)
  fit <- res$fit
  expect_true(!is.null(fit$phylo_signal_per_trait))
  expect_true(!is.null(fit$phylo_gate_triggered))
  expect_equal(fit$phylo_signal_method, "lambda")
  expect_equal(fit$phylo_signal_threshold, 0.2)
  # AVONET traits all have strong phylogenetic signal; none should be gated
  expect_false(any(fit$phylo_gate_triggered, na.rm = TRUE))
})

test_that("fit_pigauto gates weak-signal traits to (0, 0, 1) exactly", {
  skip_if_not_installed("phytools")
  set.seed(2026L)
  n <- 200L
  tree <- ape::rcoal(n)
  # White-noise trait — lambda ~ 0
  traits <- data.frame(noise = stats::rnorm(n),
                        row.names = tree$tip.label)
  # Mask some cells
  traits$noise[sample(n, 30)] <- NA_real_
  res <- pigauto::impute(traits, tree,
                           phylo_signal_gate = TRUE,
                           phylo_signal_threshold = 0.2,
                           safety_floor = TRUE,
                           epochs = 30L, n_imputations = 1L,
                           verbose = FALSE, seed = 2026L)
  fit <- res$fit
  expect_true(isTRUE(as.logical(fit$phylo_gate_triggered["noise"])))
  # r_cal_bm = 0, r_cal_gnn = 0, r_cal_mean = 1 on the noise latent col
  expect_equal(as.numeric(fit$r_cal_bm["noise"]),   0, tolerance = 1e-10)
  expect_equal(as.numeric(fit$r_cal_gnn["noise"]),  0, tolerance = 1e-10)
  expect_equal(as.numeric(fit$r_cal_mean["noise"]), 1, tolerance = 1e-10)
})
```

- [ ] **Step 2: Run test to verify fails**

Run: `cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::test(filter = "phylo-signal-gate")'`
Expected: fails — `fit_pigauto()` doesn't accept `phylo_signal_gate` etc.

- [ ] **Step 3: Add signature args + roxygen to fit_pigauto()**

Open `R/fit_pigauto.R`. Find the signature. Add three new arguments positioned near `safety_floor`:

```r
                    safety_floor = TRUE,
                    phylo_signal_gate = TRUE,
                    phylo_signal_threshold = 0.2,
                    phylo_signal_method = c("lambda", "blomberg_k"),
                    ...
```

Handle the method match:

```r
phylo_signal_method <- match.arg(phylo_signal_method)
```

Add roxygen before the function:

```r
#' @param phylo_signal_gate logical. When \code{TRUE} (default since
#'   v0.9.1.9003), compute per-trait Pagel's λ on training-observed
#'   cells before fitting; for traits with \code{λ < phylo_signal_threshold},
#'   force \code{(r_cal_bm = 0, r_cal_gnn = 0, r_cal_mean = 1)} directly
#'   and skip BM + GNN training on those traits.  Requires the
#'   \code{phytools} package.  Falls back to safety-floor-only
#'   behaviour (\code{phylo_signal_gate = FALSE} effective) when
#'   \code{phytools} is absent.
#' @param phylo_signal_threshold numeric, default \code{0.2}.  Traits
#'   with Pagel's λ below this value are routed to the grand-mean
#'   corner of the safety-floor simplex.
#' @param phylo_signal_method character, currently only \code{"lambda"}
#'   is fully implemented.  Reserved \code{"blomberg_k"} path returns
#'   Blomberg's K via \code{phytools::phylosig()} but uses the same
#'   threshold — which is NOT dimensionally comparable; users selecting
#'   K must supply a K-appropriate threshold.
```

- [ ] **Step 4: Compute phylo signal before fit_baseline() + apply gate**

Just after `preprocess_traits()` returns `data`, insert the signal computation. Find the line that reads something like `data <- preprocess_traits(...)`. Just below it, add:

```r
# Phylogenetic-signal gate: compute per-trait λ and build a "gated
# latent columns" logical mask. Gated columns get (0, 0, 1) in the
# final calibrated_gates_list after safety_floor calibration.
if (phylo_signal_gate) {
  phylo_signal_per_trait <- compute_phylo_signal_per_trait(
    data = data, tree = tree,
    method = phylo_signal_method, min_tips = 20L)
} else {
  phylo_signal_per_trait <- rep(NA_real_, length(data$trait_map))
  names(phylo_signal_per_trait) <- vapply(data$trait_map,
                                            function(tm) tm$name,
                                            character(1L))
}
phylo_gate_triggered <- phylo_signal_gate &
  !is.na(phylo_signal_per_trait) &
  (phylo_signal_per_trait < phylo_signal_threshold)
names(phylo_gate_triggered) <- names(phylo_signal_per_trait)
# Build a latent-column-level logical mask for downstream overrides.
gated_latent_cols <- integer(0L)
if (any(phylo_gate_triggered)) {
  gated_trait_names <- names(phylo_gate_triggered)[phylo_gate_triggered]
  for (tm in data$trait_map) {
    if (tm$name %in% gated_trait_names) {
      gated_latent_cols <- c(gated_latent_cols, tm$latent_cols)
    }
  }
}
```

- [ ] **Step 5: After calibration, force `r_cal_*` on gated latent columns**

Find the block where `calibrated_gates_list` is built (from Task 5 of the safety-floor plan — currently around line 680-ish). Just after the block that sets `calibrated_gates_list`, insert:

```r
# Apply phylo-signal gate override: gated latent columns get (0, 0, 1)
# regardless of what the 3-way simplex calibrator picked.
if (length(gated_latent_cols) > 0L && !is.null(calibrated_gates_list)) {
  calibrated_gates_list$r_cal_bm[gated_latent_cols]   <- 0
  calibrated_gates_list$r_cal_gnn[gated_latent_cols]  <- 0
  calibrated_gates_list$r_cal_mean[gated_latent_cols] <- 1
  calibrated_gates <- calibrated_gates_list$r_cal_gnn   # legacy scalar (now 0)
}
```

- [ ] **Step 6: Store the signal + gate slots on the fit**

Find the `structure(list(..., class = "pigauto_fit"))` at the return. Add four new slots alongside the existing safety-floor ones:

```r
  phylo_signal_per_trait = phylo_signal_per_trait,
  phylo_gate_triggered   = phylo_gate_triggered,
  phylo_signal_method    = phylo_signal_method,
  phylo_signal_threshold = phylo_signal_threshold,
```

- [ ] **Step 7: Run tests**

Run: `cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::load_all(); devtools::test(filter = "phylo-signal-gate")'`
Expected: new tests PASS; previous 3 still pass.

Also verify no regression:

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::test(filter = "safety-floor")' 2>&1 | tail -n 5
```
Expected: safety-floor filter still at `[ FAIL 0 | PASS 75 ]`.

- [ ] **Step 8: Commit**

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
git add R/fit_pigauto.R tests/testthat/test-phylo-signal-gate.R
git commit -m "phylo-signal: fit_pigauto() routes weak-signal traits to grand-mean corner"
```

---

## Task 4: Add `phylo_signal_gate` pass-through to `impute()`

**Files:**
- Modify: `R/impute.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-phylo-signal-gate.R`:

```r
test_that("impute(phylo_signal_gate = TRUE) is the default and threads to fit", {
  skip_if_not_installed("phytools")
  data("avonet300", package = "pigauto")
  data("tree300",   package = "pigauto")
  df <- avonet300
  if ("Species_Key" %in% colnames(df)) {
    rownames(df) <- df$Species_Key; df$Species_Key <- NULL
  }
  set.seed(2026L)
  df$Mass[sample(300, 30)] <- NA_real_
  # default call
  r1 <- pigauto::impute(df, tree300, epochs = 30L, n_imputations = 1L,
                          verbose = FALSE, seed = 2026L)
  expect_equal(r1$fit$phylo_signal_method, "lambda")
  # explicit FALSE
  r2 <- pigauto::impute(df, tree300, phylo_signal_gate = FALSE,
                          epochs = 30L, n_imputations = 1L,
                          verbose = FALSE, seed = 2026L)
  expect_true(all(is.na(r2$fit$phylo_signal_per_trait)))
  expect_false(any(r2$fit$phylo_gate_triggered, na.rm = TRUE))
})
```

- [ ] **Step 2: Run test to verify fails**

Run: `cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::test(filter = "phylo-signal-gate")'`

- [ ] **Step 3: Add args to impute() signature**

Open `R/impute.R`. Add three new arguments positioned near `safety_floor`:

```r
                safety_floor = TRUE,
                phylo_signal_gate = TRUE,
                phylo_signal_threshold = 0.2,
                phylo_signal_method = "lambda",
```

Forward them in the `fit_pigauto()` call body:

```r
fit <- fit_pigauto(
  data = data,
  ...
  safety_floor = safety_floor,
  phylo_signal_gate = phylo_signal_gate,
  phylo_signal_threshold = phylo_signal_threshold,
  phylo_signal_method = phylo_signal_method,
  ...
)
```

Add roxygen pass-through:

```r
#' @param phylo_signal_gate,phylo_signal_threshold,phylo_signal_method
#'   Pass-through to [fit_pigauto()]. See that help page for details.
```

- [ ] **Step 4: Run tests**

Run: `cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::load_all(); devtools::test(filter = "phylo-signal")'`

- [ ] **Step 5: Commit**

```bash
git add R/impute.R tests/testthat/test-phylo-signal-gate.R
git commit -m "phylo-signal: impute() accepts phylo_signal_gate = TRUE default"
```

---

## Task 5: Extend `print.pigauto_fit()` with phylogenetic-signal section

**Files:**
- Modify or create: `R/print_pigauto_fit.R` (check whether `print.pigauto_fit` exists first)

- [ ] **Step 1: Find the current print method**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && grep -rn "print.pigauto_fit\|^print <- function(x.*pigauto" R/ | head -n 5
```

If `print.pigauto_fit()` exists, modify it in place. If not, create `R/print_pigauto_fit.R`.

- [ ] **Step 2: Write the failing test**

Append to `tests/testthat/test-phylo-signal-gate.R`:

```r
test_that("print.pigauto_fit shows phylogenetic-signal section when gate triggered", {
  skip_if_not_installed("phytools")
  set.seed(2026L)
  n <- 200L
  tree <- ape::rcoal(n)
  traits <- data.frame(noise = stats::rnorm(n),
                        row.names = tree$tip.label)
  traits$noise[sample(n, 30)] <- NA_real_
  res <- pigauto::impute(traits, tree,
                           phylo_signal_gate = TRUE,
                           phylo_signal_threshold = 0.2,
                           epochs = 30L, n_imputations = 1L,
                           verbose = FALSE, seed = 2026L)
  out <- utils::capture.output(print(res$fit))
  expect_true(any(grepl("Phylogenetic signal", out)))
  expect_true(any(grepl("noise", out)))
  expect_true(any(grepl("gated", out, ignore.case = TRUE)))
})
```

- [ ] **Step 3: Implement the print section**

In the existing `print.pigauto_fit()` (or new one), just before the tail `invisible(x)` add:

```r
if (!is.null(x$phylo_gate_triggered) &&
    any(!is.na(x$phylo_signal_per_trait))) {
  thr <- x$phylo_signal_threshold %||% 0.2
  method <- x$phylo_signal_method %||% "lambda"
  sig <- x$phylo_signal_per_trait
  gated <- x$phylo_gate_triggered
  cat(sprintf("\nPhylogenetic signal (%s, threshold %.2f):\n", method, thr))
  if (any(gated, na.rm = TRUE)) {
    gated_names <- names(gated)[gated & !is.na(gated)]
    cat("  gated (BM skipped, using grand mean):",
         paste(sprintf("%s (%s=%.2f)", gated_names,
                       if (method == "lambda") "λ" else "K",
                       sig[gated_names]), collapse = ", "),
         "\n")
  } else {
    cat(sprintf("  all %d traits have %s >= %.2f; safety floor is the fallback.\n",
                 sum(!is.na(sig)),
                 if (method == "lambda") "λ" else "K", thr))
  }
}
```

If `print.pigauto_fit()` doesn't already exist, create `R/print_pigauto_fit.R` with a minimal implementation that emits the standard summary lines + the new section.

- [ ] **Step 4: Run test**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::load_all(); devtools::test(filter = "phylo-signal")'
```

- [ ] **Step 5: Commit**

```bash
git add R/print_pigauto_fit.R R/fit_pigauto.R tests/testthat/test-phylo-signal-gate.R
# (paths depend on which file houses print.pigauto_fit — include both if unsure)
git commit -m "phylo-signal: print.pigauto_fit() shows per-trait λ when gate triggered"
```

---

## Task 6: Plants smoke test — verify weak-signal traits are flagged

**Files:**
- Modify: `tests/testthat/test-phylo-signal-gate.R`

- [ ] **Step 1: Write the test**

```r
test_that("plants smoke: BIEN plant traits with weak phylo signal are gated", {
  skip_if_not_installed("phytools")
  cache_trait <- "script/data-cache/bien_trait_means.rds"
  cache_tree  <- "script/data-cache/bien_tree.rds"
  if (!file.exists(cache_trait)) {
    cache_trait <- file.path("/Users/z3437171/Dropbox/Github Local/pigauto",
                              "script/data-cache", "bien_trait_means.rds")
    cache_tree  <- file.path("/Users/z3437171/Dropbox/Github Local/pigauto",
                              "script/data-cache", "bien_tree.rds")
  }
  skip_if_not(file.exists(cache_trait) && file.exists(cache_tree),
              "BIEN cache not found")
  # Build wide data frame + tree (same as test-safety-floor.R plants block)
  trait_means <- readRDS(cache_trait)
  tree_raw    <- readRDS(cache_tree)
  tree_all <- if (is.list(tree_raw) && !inherits(tree_raw, "phylo")) {
    t <- tree_raw$scenario.3
    t$tip.label <- gsub("_", " ", t$tip.label)
    t
  } else {
    tree_raw
  }
  all_species <- Reduce(union, lapply(trait_means,
    function(d) if (!is.null(d)) d$species else character(0)))
  wide <- data.frame(species = all_species, stringsAsFactors = FALSE)
  for (nm in names(trait_means)) {
    d <- trait_means[[nm]]
    if (is.null(d)) { wide[[nm]] <- NA_real_; next }
    m <- match(wide$species, d$species)
    wide[[nm]] <- suppressWarnings(as.numeric(d$mean_value[m]))
  }
  matched <- intersect(wide$species, tree_all$tip.label)
  skip_if_not(length(matched) >= 500L,
              "insufficient matched species")
  set.seed(2026L)
  sp <- sample(matched, 500L)
  df <- wide[wide$species %in% sp, , drop = FALSE]
  rownames(df) <- df$species; df$species <- NULL
  tr <- ape::keep.tip(tree_all, sp)
  res <- pigauto::impute(df, tr,
                           phylo_signal_gate = TRUE,
                           phylo_signal_threshold = 0.2,
                           safety_floor = TRUE,
                           epochs = 40L, n_imputations = 5L,
                           verbose = FALSE, seed = 2026L)
  fit <- res$fit
  gated <- fit$phylo_gate_triggered
  n_gated <- sum(gated, na.rm = TRUE)
  expect_gte(n_gated, 2L)
  # gated traits have r_cal_mean = 1 exactly
  for (nm in names(gated)[gated & !is.na(gated)]) {
    latent_cols <- data$trait_map[[which(vapply(data$trait_map,
      function(tm) tm$name, character(1)) == nm)]]$latent_cols
    # (can't directly access data from here; rely on stored slots)
  }
  # Simpler assertion: at least one of the 5 plants traits gated and
  # has r_cal_mean = 1 in the stored slot.
  gated_names <- names(gated)[gated & !is.na(gated)]
  # Find any latent col corresponding to a gated trait — for continuous,
  # name == trait name.
  gated_in_r_mean <- intersect(gated_names, names(fit$r_cal_mean))
  if (length(gated_in_r_mean) > 0L) {
    expect_true(all(fit$r_cal_mean[gated_in_r_mean] == 1))
  }
})
```

- [ ] **Step 2: Run test**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::load_all(); devtools::test(filter = "phylo-signal")'
```

Expected: passes (or skips if BIEN cache absent).

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-phylo-signal-gate.R
git commit -m "phylo-signal: plants smoke test confirms weak-signal traits gated"
```

---

## Task 7: NEWS.md + DESCRIPTION bump

**Files:**
- Modify: `NEWS.md`
- Modify: `DESCRIPTION`

- [ ] **Step 1: Add NEWS section**

Prepend to `NEWS.md`:

```markdown
# pigauto 0.9.1.9003 (dev)

## Phylogenetic-signal gate

Default change since v0.9.1.9003: `impute()` / `fit_pigauto()` now
compute per-trait Pagel's λ on training-observed cells and route
traits with `λ < 0.2` to the grand-mean corner `(0, 0, 1)` of the
safety-floor simplex, skipping BM fitting and GNN training effort
on those traits. Complements the v0.9.1.9002 safety floor with an
*explicit* per-trait decision: "pigauto knows when phylogeny helps".

### Effect on existing benches

- Birds / mammals / fish / amphibians: zero traits gated (all λ ≥ 0.2).
  Results bit-identical to v0.9.1.9002.
- Plants: 3+ of 5 BIEN traits flagged as weak-signal (λ < 0.2) and
  gated to grand mean.  `fit$phylo_gate_triggered` reports which.

### API

- `impute(..., phylo_signal_gate = TRUE, phylo_signal_threshold = 0.2,
   phylo_signal_method = "lambda")` — new args, defaults chosen to
   match v0.9.1.9002 results on high-signal taxa.
- `fit_pigauto()` same pass-through.
- Fit object gains four new slots:
  `phylo_signal_per_trait` (named numeric λ values),
  `phylo_gate_triggered` (named logical),
  `phylo_signal_method`, `phylo_signal_threshold`.

### Opt-out

`phylo_signal_gate = FALSE` reproduces v0.9.1.9002 safety-floor-only
behaviour bit-identically.

### Reporting

`print.pigauto_fit()` now surfaces a "Phylogenetic signal" section
listing gated vs passed traits when any trait was gated.

### Dependencies

`phytools` added to Suggests. When absent, `phylo_signal_gate`
degrades gracefully to FALSE (safety-floor-only) with a one-time
warning.

### Files

- `R/phylo_signal.R` — new, per-trait λ computation.
- `R/fit_pigauto.R`, `R/impute.R`, `R/print_pigauto_fit.R` — modified.
- `specs/2026-04-23-phylo-signal-gate-design.md` — design spec.
- `plans/2026-04-23-phylo-signal-gate.md` — implementation plan.
```

- [ ] **Step 2: Bump DESCRIPTION**

Change `Version: 0.9.1.9002` → `Version: 0.9.1.9003`.

- [ ] **Step 3: Commit**

```bash
git add NEWS.md DESCRIPTION
git commit -m "phylo-signal: NEWS.md entry + DESCRIPTION bump to 0.9.1.9003"
```

---

## Task 8: Final R CMD check + push + open PR

**Files:**
- Observe only

- [ ] **Step 1: Regenerate man pages**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::document()'
```

Commit any stale man/ changes:

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
git status --short
git add man/ NAMESPACE
git commit -m "phylo-signal: regenerate man pages + NAMESPACE"
```

If nothing changed, skip this commit.

- [ ] **Step 2: Run full test suite**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::test()' > /tmp/phylo_suite.log 2>&1; grep "^\[ FAIL" /tmp/phylo_suite.log | tail -n 1
```

Expected: `[ FAIL 0 | ... | PASS N ]` where N includes the ~8 new phylo-signal test expectations on top of the 934 existing.

- [ ] **Step 3: R CMD check clean**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && Rscript -e 'devtools::check(error_on = "error")' 2>&1 | tail -n 5
```

Expected: 0 errors, 0 warnings, pre-existing notes.

- [ ] **Step 4: Push branch**

```
cd "/Users/z3437171/Dropbox/Github Local/pigauto" && git push -u origin feature/phylo-signal-gate
```

- [ ] **Step 5: Open PR**

```bash
gh pr create --base main --head feature/phylo-signal-gate --title "phylo-signal gate: per-trait λ routes weak-signal traits to grand mean (v0.9.1.9003)" --body "$(cat <<'EOF'
## Summary
- Per-trait Pagel's λ diagnostic computed on training-observed cells.
- Traits with λ < 0.2 routed directly to grand-mean corner (0, 0, 1).
- Complements PR #43's safety floor with explicit per-trait decisions.
- phytools moves to Suggests; graceful degradation when absent.

## Test plan
- [x] New tests in tests/testthat/test-phylo-signal-gate.R (~8 blocks).
- [x] Existing safety-floor + full-suite tests unchanged.
- [x] R CMD check 0/0/N clean.
- [x] AVONET regression: no trait gated (all λ > 0.9).
- [x] Plants smoke: 3+ of 5 BIEN traits gated as expected.

## Stacks on
PR #43 (feature/safety-floor-mean-gate).  When #43 merges, rebase this
onto main and merge.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 6: Report PR URL**

---

## Self-review

**1. Spec coverage:** Every section of
`specs/2026-04-23-phylo-signal-gate-design.md` maps to tasks:
- §4.1 two-stage decision → Tasks 1, 3, 4
- §4.2 latent-col-to-trait mapping → Task 1 (zi_count coupled gate)
- §4.3 when to compute → Task 3 (at top of fit_pigauto)
- §4.4 discrete traits → Task 1 (.phylo_signal_lambda_discrete)
- §4.5 storage on pigauto_fit → Task 3 (4 new slots)
- §4.6 fit-path optimisation → documented but not aggressively
  enforced (safe to defer)
- §4.7 API → Tasks 3, 4
- §4.8 reporting (print method) → Task 5
- §5 testing → Tasks 1-6
- §6 risks → min_tips guard Task 1; phytools Suggests Task 2;
  graceful fallback Task 3
- §7 rollout → Task 8
- §8 success metric → Task 6 plants smoke + Task 8 full canary

**2. Placeholder scan:** no "TBD" / "TODO". Every step has concrete
code or commands.

**3. Type consistency:** `compute_phylo_signal_per_trait()` signature
identical across Tasks 1, 3, 4. `phylo_gate_triggered` name identical
across Tasks 3, 5, 6. `phylo_signal_per_trait` identical across 3, 5, 6.

No issues found.

---

## Execution handoff

**Plan complete. Saved to `plans/2026-04-23-phylo-signal-gate.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — dispatch fresh subagent per task, two-stage review.

**2. Inline execution** — via executing-plans skill.

Default: Subagent-Driven.
