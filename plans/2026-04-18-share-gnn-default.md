# share_gnn=TRUE default Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `share_gnn = TRUE` the default in `multi_impute_trees()` so that T=50 posterior trees × m_per_tree=1 = M=50 datasets (the N&dV 2019 canonical workflow) is the cheap, default path at n=10k species.

**Architecture:** Fit pigauto ONCE on a reference tree (MCC via `phangorn`, fallback to `trees[[1]]`, user-overridable). For each posterior tree, recompute only the BM baseline and reuse the trained GNN delta via a new `baseline_override` argument on `predict.pigauto_fit()`. Gate safety means the shared-GNN approximation is lossless when the gate is closed and conservative when open.

**Tech Stack:** R 4.x, `testthat` 3rd edition, `roxygen2`, `devtools`, `phangorn` (new Suggests), `pigauto` 0.9.0.9000.

**Working branch:** `feature/share-gnn-default` (already created and carrying the spec commit).

**Files touched:**
| Path | Purpose | Create/Modify |
|---|---|---|
| `DESCRIPTION` | add `phangorn` to Suggests | modify |
| `R/predict_pigauto.R` | accept `baseline_override = NULL` in `predict.pigauto_fit()` | modify (L82–144 region) |
| `R/multi_impute_trees.R` | new signature args `share_gnn`, `reference_tree`; new internal helpers `resolve_reference_tree()` and `predict_on_new_tree()`; shared-path loop; updated roxygen | modify (top to bottom) |
| `R/multi_impute.R` | add `@section When to use this:` roxygen block pointing at the two-function decision | modify (roxygen header only) |
| `tests/testthat/test-share-gnn.R` | unit tests for the new behaviour | create |
| `vignettes/tree-uncertainty.Rmd` | new pkgdown article on multi-tree MI | create |
| `_pkgdown.yml` | navbar entry for the new vignette | modify |
| `NEWS.md` | v0.9.1 entry documenting the behaviour change | modify |
| `script/bench_share_gnn.R` | non-blocking smoke benchmark | create |
| `NAMESPACE` | regenerated by `devtools::document()` if any new `@export` appears (none planned, so NAMESPACE should stay clean) | regenerate |

---

## Task 1: Add `phangorn` to Suggests

**Files:**
- Modify: `DESCRIPTION`

- [ ] **Step 1: Edit the Suggests list**

Change the `Suggests:` block so it reads (insert `phangorn` alphabetically between `pkgdown` and `RSpectra`, or add to the end — existing style groups by rough importance so append at end):

```
Suggests:
    testthat,
    knitr,
    rmarkdown,
    pkgdown,
    RSpectra,
    Rphylopars,
    BACE,
    MCMCglmm,
    glmmTMB,
    phangorn
```

- [ ] **Step 2: Verify DESCRIPTION parses**

Run:

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
Rscript -e 'd <- read.dcf("DESCRIPTION"); cat("Suggests contains phangorn:", grepl("phangorn", d[1,"Suggests"]), "\n")'
```

Expected: `Suggests contains phangorn: TRUE`

- [ ] **Step 3: Commit**

```bash
git add DESCRIPTION
git commit -m "DESCRIPTION: add phangorn to Suggests (for MCC tree selection in multi_impute_trees)"
```

---

## Task 2: Add `baseline_override` argument to `predict.pigauto_fit()`

**Files:**
- Modify: `R/predict_pigauto.R:82-144` (function signature + the block that reads `object$baseline$mu` and `object$baseline$se`)
- Test: `tests/testthat/test-share-gnn.R`

- [ ] **Step 1: Write a failing test that pins down the behaviour**

Create `tests/testthat/test-share-gnn.R` with this content:

```r
test_that("predict.pigauto_fit accepts baseline_override and uses it in place of object$baseline", {
  skip_if_not_installed("torch")
  skip_if_not(torch::torch_is_installed(), "libtorch not installed")

  set.seed(42L)
  tree <- ape::rcoal(30L)
  df   <- pigauto::simulate_bm_traits(tree, n_traits = 2L, seed = 1L)

  pd    <- pigauto::preprocess_traits(df, tree, log_transform = FALSE)
  spl   <- pigauto::make_missing_splits(pd$X_scaled, missing_frac = 0.20,
                                         seed = 1L, trait_map = pd$trait_map)
  graph <- pigauto::build_phylo_graph(tree, k_eigen = 4L)
  bl    <- pigauto::fit_baseline(pd, tree, splits = spl, graph = graph)
  fit   <- pigauto::fit_pigauto(pd, tree, splits = spl, baseline = bl, graph = graph,
                                 epochs = 30L, verbose = FALSE, seed = 1L)

  # default: uses object$baseline
  pred_default <- stats::predict(fit, return_se = FALSE, n_imputations = 1L)
  expect_s3_class(pred_default, "pigauto_pred")

  # override with a shifted baseline -- predictions must differ
  bl2   <- bl
  bl2$mu <- bl$mu + 5.0
  pred_shifted <- stats::predict(fit, return_se = FALSE, n_imputations = 1L,
                                  baseline_override = bl2)
  expect_s3_class(pred_shifted, "pigauto_pred")

  # Imputed latent values should reflect the shift (gate is open at some level)
  expect_false(
    identical(pred_default$imputed_latent, pred_shifted$imputed_latent),
    info = "baseline_override should change predictions when gate is not fully closed"
  )
})
```

- [ ] **Step 2: Run the test to verify it fails**

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: test errors with `unused argument (baseline_override = bl2)`.

- [ ] **Step 3: Add the parameter to `predict.pigauto_fit()`**

Edit `R/predict_pigauto.R` at the function signature (line 82):

```r
predict.pigauto_fit <- function(object, newdata = NULL, return_se = TRUE,
                                n_imputations = 1L, baseline_override = NULL,
                                ...) {
```

Then, shortly after the function extracts `trait_map`, add (insert immediately before the line `MU_species <- object$baseline$mu`):

```r
  # Optional baseline override — used by multi_impute_trees(share_gnn = TRUE)
  # to reuse a trained GNN across posterior trees, with only the BM baseline
  # recomputed per tree.
  effective_baseline <- if (!is.null(baseline_override)) baseline_override
                        else object$baseline
```

Then replace subsequent `object$baseline$mu` → `effective_baseline$mu` and
`object$baseline$se` → `effective_baseline$se` within the function body.
Grep first to find the occurrences:

```bash
Rscript -e 'cat(readLines("R/predict_pigauto.R")[grep("object\\$baseline", readLines("R/predict_pigauto.R"))])'
```

Replace each one with `effective_baseline$...`.

- [ ] **Step 4: Update the roxygen docs at lines 60-78**

Add a new `@param` line (alphabetically among the others):

```r
#' @param baseline_override optional `list(mu, se)` with the same shape as
#'   `object$baseline`. When supplied, predictions use this baseline instead
#'   of the one saved in the fit. Used internally by [multi_impute_trees()]
#'   to reuse a trained GNN across posterior trees. Most users can ignore
#'   this. Default `NULL` (use the fit's own baseline).
```

- [ ] **Step 5: Regenerate docs**

```bash
Rscript -e 'devtools::document()'
```

Expected: `Writing predict.pigauto_fit.Rd`.

- [ ] **Step 6: Run the test again — should pass**

```bash
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: 1 test passes.

- [ ] **Step 7: Run the full test suite to make sure nothing else broke**

```bash
Rscript -e 'devtools::test()'
```

Expected: all tests pass (558 → 559). No FAILs.

- [ ] **Step 8: Commit**

```bash
git add R/predict_pigauto.R man/predict.pigauto_fit.Rd tests/testthat/test-share-gnn.R
git commit -m "predict.pigauto_fit: add baseline_override argument

Lets callers swap in a freshly-computed baseline while keeping the
trained GNN delta constant. Enables multi_impute_trees(share_gnn = TRUE)
by letting the fit trained on the reference tree be reused across
posterior trees with only the baseline recomputed per tree."
```

---

## Task 3: Add `resolve_reference_tree()` helper

**Files:**
- Modify: `R/multi_impute_trees.R` (new internal function inside this file)
- Test: `tests/testthat/test-share-gnn.R`

- [ ] **Step 1: Write failing tests for the helper**

Append to `tests/testthat/test-share-gnn.R`:

```r
test_that("resolve_reference_tree returns user-supplied tree when given", {
  trees <- list(ape::rcoal(20L, tip.label = paste0("t", 1:20)),
                ape::rcoal(20L, tip.label = paste0("t", 1:20)),
                ape::rcoal(20L, tip.label = paste0("t", 1:20)))
  user_tree <- ape::rcoal(20L, tip.label = paste0("t", 1:20))

  out <- pigauto:::resolve_reference_tree(trees, reference_tree = user_tree)
  expect_identical(out, user_tree)
})

test_that("resolve_reference_tree uses phangorn MCC when phangorn available", {
  skip_if_not_installed("phangorn")

  set.seed(1L)
  trees <- lapply(1:5, function(i) ape::rcoal(20L, tip.label = paste0("t", 1:20)))
  class(trees) <- "multiPhylo"

  out <- pigauto:::resolve_reference_tree(trees, reference_tree = NULL)
  expect_s3_class(out, "phylo")
  # MCC must be identical (in structure) to one of the input trees
  any_match <- any(vapply(trees, function(tr) {
    isTRUE(all.equal(tr$edge, out$edge)) &&
    isTRUE(all.equal(tr$tip.label, out$tip.label))
  }, logical(1)))
  expect_true(any_match, info = "MCC tree must be one of the input trees")
})

test_that("resolve_reference_tree warns and falls back when phangorn missing", {
  trees <- list(ape::rcoal(15L), ape::rcoal(15L))
  local_mocked_bindings(
    requireNamespace = function(pkg, ...) !(identical(pkg, "phangorn")),
    .package = "base"
  )

  expect_warning(
    out <- pigauto:::resolve_reference_tree(trees, reference_tree = NULL),
    "phangorn"
  )
  expect_identical(out, trees[[1]])
})
```

- [ ] **Step 2: Run — should fail (function doesn't exist)**

```bash
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: errors with `object 'resolve_reference_tree' not found`.

- [ ] **Step 3: Add the helper to `R/multi_impute_trees.R`**

Insert near the top of the file, after the roxygen header for the exported function but before `multi_impute_trees <- function(...)`:

```r
# Pick the reference tree used to train the GNN when `share_gnn = TRUE`.
#
# - `reference_tree` non-NULL: return it unchanged.
# - `reference_tree` NULL + phangorn installed: compute MCC via
#   `phangorn::maxCladeCred(trees)`.
# - phangorn missing: warn and fall back to `trees[[1]]`.
resolve_reference_tree <- function(trees, reference_tree = NULL) {
  if (!is.null(reference_tree)) {
    if (!inherits(reference_tree, "phylo")) {
      stop("`reference_tree` must be a 'phylo' object. Got ",
           class(reference_tree)[1], ".", call. = FALSE)
    }
    return(reference_tree)
  }
  if (requireNamespace("phangorn", quietly = TRUE)) {
    tr_multi <- trees
    if (!inherits(tr_multi, "multiPhylo")) class(tr_multi) <- "multiPhylo"
    return(phangorn::maxCladeCred(tr_multi))
  }
  warning("phangorn required for MCC tree selection; falling back to ",
          "trees[[1]]. Install with install.packages('phangorn') for ",
          "MCC-based selection.", call. = FALSE)
  trees[[1]]
}
```

- [ ] **Step 4: Re-run tests**

```bash
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: all four tests in test-share-gnn.R pass (including the first predict test from Task 2).

- [ ] **Step 5: Commit**

```bash
git add R/multi_impute_trees.R tests/testthat/test-share-gnn.R
git commit -m "multi_impute_trees: add resolve_reference_tree() internal helper

Picks the tree that share_gnn=TRUE will train the GNN on: user-supplied
if given, else maximum-clade-credibility via phangorn, else trees[[1]]
with a warning."
```

---

## Task 4: Add `share_gnn` + `reference_tree` args and the shared-path loop

**Files:**
- Modify: `R/multi_impute_trees.R` (signature, main body)
- Test: `tests/testthat/test-share-gnn.R`

- [ ] **Step 1: Write a failing test pinning down the `share_gnn = TRUE` flow**

Append to `tests/testthat/test-share-gnn.R`:

```r
test_that("multi_impute_trees(share_gnn=TRUE) returns a single shared fit", {
  skip_if_not_installed("torch")
  skip_if_not(torch::torch_is_installed(), "libtorch not installed")

  set.seed(7L)
  n <- 20L
  trees <- lapply(1:3, function(i) ape::rcoal(n, tip.label = paste0("sp", 1:n)))
  df <- pigauto::simulate_bm_traits(trees[[1]], n_traits = 2L, seed = 1L)
  # artificial missingness
  df$trait1[c(1, 5, 9)] <- NA
  df$trait2[c(2, 6, 10)] <- NA

  mi_shared <- pigauto::multi_impute_trees(
    df, trees, m_per_tree = 1L,
    share_gnn = TRUE, reference_tree = trees[[1]],
    epochs = 30L, verbose = FALSE, seed = 1L
  )

  expect_s3_class(mi_shared, "pigauto_mi_trees")
  expect_equal(mi_shared$m, 3L)                       # T * m_per_tree
  expect_equal(length(mi_shared$datasets), 3L)
  expect_equal(mi_shared$tree_index, 1:3)
  expect_true(mi_shared$share_gnn)
  expect_false(is.null(mi_shared$fit))                # single shared fit
  expect_null(mi_shared$fits)                         # old list absent under share
  expect_identical(mi_shared$reference_tree, trees[[1]])
})

test_that("multi_impute_trees(share_gnn=FALSE) keeps per-tree behaviour", {
  skip_if_not_installed("torch")
  skip_if_not(torch::torch_is_installed(), "libtorch not installed")

  set.seed(7L)
  n <- 20L
  trees <- lapply(1:2, function(i) ape::rcoal(n, tip.label = paste0("sp", 1:n)))
  df <- pigauto::simulate_bm_traits(trees[[1]], n_traits = 2L, seed = 1L)
  df$trait1[c(1, 5)] <- NA

  mi_legacy <- pigauto::multi_impute_trees(
    df, trees, m_per_tree = 1L,
    share_gnn = FALSE,
    epochs = 30L, verbose = FALSE, seed = 1L
  )

  expect_s3_class(mi_legacy, "pigauto_mi_trees")
  expect_false(mi_legacy$share_gnn)
  expect_equal(length(mi_legacy$fits), 2L)            # per-tree list present
  expect_null(mi_legacy$fit)                          # single fit absent
})
```

- [ ] **Step 2: Run — should fail**

```bash
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: errors about `unused argument (share_gnn = TRUE)`.

- [ ] **Step 3: Add `share_gnn` and `reference_tree` to the signature**

Edit `R/multi_impute_trees.R`. Change the signature (currently line 163) from:

```r
multi_impute_trees <- function(traits, trees, m_per_tree = 5L,
                               species_col = NULL,
                               trait_types = NULL,
                               multi_proportion_groups = NULL,
                               log_transform = TRUE,
                               missing_frac = 0.25,
                               covariates = NULL,
                               epochs = 2000L, verbose = TRUE,
                               seed = 1L, ...) {
```

to:

```r
multi_impute_trees <- function(traits, trees, m_per_tree = 1L,
                               species_col = NULL,
                               trait_types = NULL,
                               multi_proportion_groups = NULL,
                               log_transform = TRUE,
                               missing_frac = 0.25,
                               covariates = NULL,
                               epochs = 2000L, verbose = TRUE,
                               seed = 1L,
                               share_gnn = TRUE,
                               reference_tree = NULL,
                               ...) {
```

Note the two default changes: `m_per_tree = 5L → 1L` and the two new args at the end.

- [ ] **Step 4: Add a low-M runtime warning**

Immediately after the existing `m_per_tree <- as.integer(m_per_tree)` validation block, add:

```r
  # Warn if total datasets M = T * m_per_tree is small (Rubin's rules wobble)
  if (T_trees * m_per_tree < 10L) {
    warning("Low total imputations (T * m_per_tree = ",
            T_trees * m_per_tree,
            "). Consider m_per_tree = 5L for stable Rubin's rules pooling ",
            "when T < 20.", call. = FALSE)
  }
```

(Place this AFTER `T_trees <- length(trees)` is defined, or move the validation ordering so `T_trees` is known here.)

- [ ] **Step 5: Branch the main loop on `share_gnn`**

Replace the body from `# ---- Run impute() on each tree ---` (currently line 207) onwards with a dispatch:

```r
  # ---- Dispatch on share_gnn ------------------------------------------------
  if (isTRUE(share_gnn)) {
    out <- run_shared_gnn(
      traits = traits, trees = trees, m_per_tree = m_per_tree,
      species_col = species_col, trait_types = trait_types,
      multi_proportion_groups = multi_proportion_groups,
      log_transform = log_transform, missing_frac = missing_frac,
      covariates = covariates, epochs = as.integer(epochs),
      verbose = verbose, seed = seed,
      reference_tree = resolve_reference_tree(trees, reference_tree),
      ...
    )
  } else {
    out <- run_per_tree(
      traits = traits, trees = trees, m_per_tree = m_per_tree,
      species_col = species_col, trait_types = trait_types,
      multi_proportion_groups = multi_proportion_groups,
      log_transform = log_transform, missing_frac = missing_frac,
      covariates = covariates, epochs = as.integer(epochs),
      verbose = verbose, seed = seed, ...
    )
  }

  out$share_gnn      <- isTRUE(share_gnn)
  out$reference_tree <- if (isTRUE(share_gnn)) resolve_reference_tree(trees, reference_tree) else NULL
  out$trees          <- trees
  out$species_col    <- species_col
  class(out) <- c("pigauto_mi_trees", "pigauto_mi")
  out
```

- [ ] **Step 6: Extract the current per-tree loop into `run_per_tree()`**

Add a non-exported `run_per_tree()` function inside the same file, containing the existing loop body (what used to follow `# ---- Run impute() on each tree`). The function returns a list with `datasets`, `m`, `n_trees`, `m_per_tree`, `tree_index`, `pooled_point`, `se`, `imputed_mask`, `fits` (the existing per-tree fit list).

- [ ] **Step 7: Add `run_shared_gnn()`**

Add a new non-exported function:

```r
run_shared_gnn <- function(traits, trees, m_per_tree,
                           species_col, trait_types, multi_proportion_groups,
                           log_transform, missing_frac, covariates,
                           epochs, verbose, seed, reference_tree, ...) {
  T_trees <- length(trees)
  M_total <- T_trees * m_per_tree
  if (verbose) {
    cat(sprintf("multi_impute_trees (share_gnn=TRUE): %d trees x %d imputations = %d datasets\n",
                T_trees, m_per_tree, M_total))
    cat("  Training GNN once on reference tree...\n")
  }

  # Fit on the reference tree — this is the expensive step, done ONCE.
  res_ref <- impute(
    traits = traits, tree = reference_tree,
    species_col = species_col, trait_types = trait_types,
    multi_proportion_groups = multi_proportion_groups,
    log_transform = log_transform, missing_frac = missing_frac,
    n_imputations = m_per_tree, covariates = covariates,
    epochs = as.integer(epochs), verbose = FALSE, seed = as.integer(seed), ...
  )
  fit_ref   <- res_ref$fit
  data_ref  <- res_ref$data
  splits_ref <- res_ref$splits
  graph_ref  <- res_ref$graph

  all_datasets <- vector("list", M_total)
  tree_index   <- integer(M_total)
  imputed_mask <- NULL
  se_sum       <- NULL
  pooled_sum   <- NULL
  idx <- 0L

  for (t in seq_len(T_trees)) {
    if (verbose) cat(sprintf("  Tree %d/%d: baseline only...", t, T_trees))
    t0 <- proc.time()

    baseline_t <- fit_baseline(data_ref, trees[[t]], splits = splits_ref,
                               graph = graph_ref)
    pred_t <- stats::predict(fit_ref, return_se = TRUE,
                              n_imputations = m_per_tree,
                              baseline_override = baseline_t)

    # Build completed data.frames (reuse build_completed)
    for (k in seq_len(m_per_tree)) {
      idx <- idx + 1L
      one <- if (!is.null(pred_t$imputed_datasets)) pred_t$imputed_datasets[[k]]
             else pred_t$imputed
      info <- build_completed(traits, one, species_col)
      all_datasets[[idx]] <- info$completed
      tree_index[idx]     <- t
      if (is.null(imputed_mask)) imputed_mask <- info$imputed_mask
    }

    # Running sums for pooled point / se
    if (!is.null(pred_t$imputed)) {
      m_imp <- as.matrix(pred_t$imputed)
      pooled_sum <- if (is.null(pooled_sum)) m_imp else pooled_sum + m_imp
    }
    if (!is.null(pred_t$se)) {
      m_se <- as.matrix(pred_t$se)
      se_sum <- if (is.null(se_sum)) m_se else se_sum + m_se
    }

    if (verbose) {
      elapsed <- (proc.time() - t0)[["elapsed"]]
      cat(sprintf(" done (%.1fs)\n", elapsed))
    }
  }

  pooled_point <- if (!is.null(pooled_sum)) as.data.frame(pooled_sum / T_trees) else NULL
  pooled_se    <- if (!is.null(se_sum))    se_sum / T_trees                   else NULL

  list(
    datasets     = all_datasets,
    m            = M_total,
    n_trees      = T_trees,
    m_per_tree   = m_per_tree,
    tree_index   = tree_index,
    pooled_point = pooled_point,
    se           = pooled_se,
    imputed_mask = imputed_mask,
    fit          = fit_ref,
    fits         = NULL
  )
}
```

- [ ] **Step 8: Run the tests**

```bash
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: all share-gnn tests pass.

- [ ] **Step 9: Run the full test suite**

```bash
Rscript -e 'devtools::test()'
```

Expected: no FAILs. One EXISTING test that checked the old `mi$fits` length under default behaviour may need updating — if it fails, locate it, confirm it's asserting the old behaviour, and change the assertion to expect `length(mi$fits) == T_trees` with `share_gnn = FALSE` explicitly set on the call. Do not paper over real failures.

- [ ] **Step 10: Commit**

```bash
git add R/multi_impute_trees.R tests/testthat/test-share-gnn.R
git commit -m "multi_impute_trees: implement share_gnn=TRUE default path

Shared-GNN flow: fit pigauto once on reference tree, then recompute
only the BM baseline per posterior tree and reuse the trained GNN via
predict.pigauto_fit(baseline_override=...). ~10-15x speedup at n=10k
vs the per-tree full-fit path (now opt-in via share_gnn=FALSE).

Default m_per_tree also flipped 5L -> 1L to align with the N&dV 2019
canonical workflow (T=50, M=50). Low-M runtime warning added."
```

---

## Task 5: Update roxygen on `multi_impute_trees()` and `multi_impute()`

**Files:**
- Modify: `R/multi_impute_trees.R` (roxygen header)
- Modify: `R/multi_impute.R` (roxygen header)

- [ ] **Step 1: Add `@param share_gnn` and `@param reference_tree` to `multi_impute_trees()`**

Insert these `@param` lines near the bottom of the existing roxygen block, just before `@param ...`:

```r
#' @param share_gnn logical. If `TRUE` (default), fit the GNN once on a
#'   reference tree and reuse it across all posterior trees, recomputing
#'   only the BM baseline per tree. Gives a ~10-15x speedup at n=10k.
#'   See the "Share-GNN" section below for tree-uncertainty propagation
#'   details. Set `FALSE` to fit from scratch on every tree (the pre-v0.9.1
#'   behaviour) when you need exact tree-by-tree model independence.
#' @param reference_tree optional `phylo` used as the training tree when
#'   `share_gnn = TRUE`. Default `NULL` selects the maximum-clade-credibility
#'   tree via `phangorn::maxCladeCred(trees)`. If `phangorn` is not
#'   installed, falls back to `trees[[1]]` with a warning.
```

- [ ] **Step 2: Add a `@section Share-GNN:` block**

Insert immediately before `@references` in the same roxygen block:

```r
#' @section Share-GNN (tree-sharing) mode:
#'
#' Under `share_gnn = TRUE` the GNN weights and spectral features are
#' trained once on the reference tree (MCC by default). For each
#' posterior tree the BM / joint-MVN baseline is recomputed, and the
#' prediction is the blend `(1 - r_cal) * baseline_t + r_cal * gnn_shared`.
#' Because `r_cal` is calibrated once on held-out data at the reference
#' tree and applied uniformly, the tree-uncertainty contribution is:
#'
#' * Fully preserved when the gate is closed (r_cal near 0): the GNN
#'   contributes nothing, and the baseline varies per tree.
#' * Partially preserved when the gate is open: the baseline portion
#'   still varies, but the GNN portion is a tree-invariant constant —
#'   this slightly under-estimates tree variance in the GNN channel.
#' * Lost in the GNN channel when the gate is fully open (rare on real
#'   data; the baseline channel still carries tree variation).
#'
#' On every real dataset benchmarked in the v0.9.0 campaign the gate
#' closed partially or fully, so `share_gnn = TRUE` is cheap AND honest.
#' Set `share_gnn = FALSE` if you need exact per-tree model independence.
```

- [ ] **Step 3: Add a `@section When to use this:` block to both functions**

Insert at the top of the roxygen body for BOTH `R/multi_impute_trees.R` and `R/multi_impute.R`, immediately after the `@description`:

```r
#' @section When to use this:
#'
#' pigauto provides two multiple-imputation functions. Pick based on how
#' many trees you have:
#'
#' * **One tree** (single published phylogeny, single time-calibrated tree):
#'   use [multi_impute()]. The `m` MC-dropout imputations capture model
#'   uncertainty.
#' * **Multiple posterior trees** (BirdTree samples, BEAST posterior, etc.):
#'   use [multi_impute_trees()]. Between-tree variation is added to the
#'   pooled SEs via Rubin's rules (Nakagawa & de Villemereuil 2019).
#'
#' The two functions share the same downstream API — both return objects
#' compatible with [with_imputations()] and [pool_mi()].
```

- [ ] **Step 4: Update `@examples` in `multi_impute_trees()`**

Replace the existing example block with one that shows the canonical T=50, m_per_tree=1 workflow (it may already show m_per_tree=5; change to the new default). Keep the two-step `Map()` construction; just update the argument.

- [ ] **Step 5: Regenerate docs**

```bash
Rscript -e 'devtools::document()'
```

Expected: `Writing multi_impute.Rd` and `Writing multi_impute_trees.Rd`.

- [ ] **Step 6: Confirm man pages rendered**

```bash
grep -l "share_gnn\|When to use this" man/multi_impute*.Rd
```

Expected: both `man/multi_impute.Rd` and `man/multi_impute_trees.Rd` show up.

- [ ] **Step 7: Commit**

```bash
git add R/multi_impute.R R/multi_impute_trees.R man/multi_impute.Rd man/multi_impute_trees.Rd
git commit -m "multi_impute*: document share_gnn + reference_tree; add decision guide"
```

---

## Task 6: NEWS.md entry

**Files:**
- Modify: `NEWS.md` (prepend a new section at the top of the dev block)

- [ ] **Step 1: Add new sub-section**

At the top of `# pigauto 0.9.0.9000 (dev)`, immediately after the header line, prepend:

```markdown
## Tree-sharing GNN is now the default in `multi_impute_trees()`

- New default `share_gnn = TRUE` in `multi_impute_trees()`. The GNN is
  trained once on a reference tree (MCC via `phangorn::maxCladeCred`,
  fallback `trees[[1]]`) and reused across posterior trees, with only
  the BM baseline recomputed per tree. ~10-15× speedup at n=10k × T=50
  makes the Nakagawa & de Villemereuil (2019) canonical workflow
  (T=50, m_per_tree=1, M=50 pooled via Rubin's rules) the default path.
- Default `m_per_tree` flipped from `5L` to `1L` to align with the N&dV
  2019 canonical workflow. Users with `T < 20` get a runtime warning
  suggesting they bump `m_per_tree` to keep Rubin's rules stable.
- Opt-out: `share_gnn = FALSE` restores the pre-v0.9.1 per-tree fit
  path for users who need exact per-tree model independence.
- New optional arg `reference_tree` lets users override the MCC choice.
- `predict.pigauto_fit()` gains a `baseline_override` argument (mostly
  internal — used by the shared-GNN path).
- New Suggests: `phangorn` (for `maxCladeCred()`).
- Tree-uncertainty propagation analysis: when the calibrated gate
  closes (the common case on real data — see the v0.9.0 validation
  suite), the shared-GNN approximation is lossless. When the gate is
  open, tree variance is slightly under-estimated in the GNN channel
  only — the baseline channel still carries it.
```

- [ ] **Step 2: Commit**

```bash
git add NEWS.md
git commit -m "NEWS: document share_gnn=TRUE default + m_per_tree=1 workflow change"
```

---

## Task 7: Smoke benchmark `script/bench_share_gnn.R`

**Files:**
- Create: `script/bench_share_gnn.R`

- [ ] **Step 1: Write the smoke script**

```r
#!/usr/bin/env Rscript
#
# script/bench_share_gnn.R
#
# Smoke benchmark: multi_impute_trees(share_gnn=TRUE) vs share_gnn=FALSE.
# Measures wall time + pooled FMI to verify the documented tradeoff
# (~10x speedup with tree uncertainty preserved when the gate closes).
#
# Tiny by design -- this is a pkgdown-linked sanity artefact, not the
# full tree_uncertainty benchmark (which still uses share_gnn=FALSE to
# anchor the v0.9.0 baseline comparison).

options(warn = 1, stringsAsFactors = FALSE)
suppressPackageStartupMessages({
  library(ape)
  devtools::load_all("/Users/z3437171/Dropbox/Github Local/pigauto", quiet = TRUE)
})

out_rds <- "script/bench_share_gnn.rds"
out_md  <- "script/bench_share_gnn.md"

n <- 100L          # small enough to be a smoke test
T_trees <- 5L      # small posterior
n_reps <- 2L       # paired reps for noise reduction

log <- function(...) cat(format(Sys.time(), "[%H:%M:%S] "), ..., "\n", sep = "")

timed <- function(expr) {
  t0 <- proc.time()
  val <- force(expr)
  list(val = val, wall = (proc.time() - t0)[["elapsed"]])
}

rows <- list()
for (r in seq_len(n_reps)) {
  set.seed(100L + r)
  trees <- lapply(seq_len(T_trees), function(i) ape::rcoal(n, tip.label = paste0("sp", 1:n)))
  df <- simulate_bm_traits(trees[[1]], n_traits = 3L, seed = 100L + r)
  df$trait1[sample.int(n, n * 0.2)] <- NA
  df$trait2[sample.int(n, n * 0.2)] <- NA

  log(sprintf("rep %d: share_gnn=TRUE", r))
  t1 <- timed(multi_impute_trees(df, trees, m_per_tree = 1L,
                                  share_gnn = TRUE, reference_tree = trees[[1]],
                                  epochs = 100L, verbose = FALSE, seed = r))
  rows[[length(rows) + 1L]] <- data.frame(
    rep = r, method = "share_gnn=TRUE", wall_s = t1$wall,
    m = t1$val$m, stringsAsFactors = FALSE
  )

  log(sprintf("rep %d: share_gnn=FALSE", r))
  t2 <- timed(multi_impute_trees(df, trees, m_per_tree = 1L,
                                  share_gnn = FALSE,
                                  epochs = 100L, verbose = FALSE, seed = r))
  rows[[length(rows) + 1L]] <- data.frame(
    rep = r, method = "share_gnn=FALSE", wall_s = t2$wall,
    m = t2$val$m, stringsAsFactors = FALSE
  )
}

results <- do.call(rbind, rows)
saveRDS(list(results = results, n = n, T_trees = T_trees), out_rds)

md <- c(
  "# share_gnn smoke benchmark",
  "",
  sprintf("n = %d, T = %d trees, m_per_tree = 1, reps = %d.", n, T_trees, n_reps),
  "",
  "| rep | method | wall (s) | M |",
  "|---|---|---|---|",
  apply(results, 1, function(r) {
    sprintf("| %s | %s | %.1f | %s |", r[["rep"]], r[["method"]], as.numeric(r[["wall_s"]]), r[["m"]])
  }),
  "",
  sprintf("Mean wall time ratio (FALSE / TRUE): %.2fx",
          mean(results$wall_s[results$method == "share_gnn=FALSE"]) /
          mean(results$wall_s[results$method == "share_gnn=TRUE"]))
)
writeLines(md, out_md)
log("done")
```

- [ ] **Step 2: Run it once to verify it works**

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
Rscript script/bench_share_gnn.R
```

Expected: completes within ~5 min on a laptop; writes `script/bench_share_gnn.rds` + `.md`. The ratio in the md should be clearly greater than 1 (share_gnn=TRUE is faster).

- [ ] **Step 3: Commit (script + RDS/md artefacts)**

```bash
git add script/bench_share_gnn.R script/bench_share_gnn.md
# Note: *.rds is gitignored, so the rds won't be staged. That's fine.
git commit -m "bench: add share_gnn smoke benchmark (share_gnn=TRUE vs FALSE)"
```

---

## Task 8: New vignette `vignettes/tree-uncertainty.Rmd`

**Files:**
- Create: `vignettes/tree-uncertainty.Rmd`

- [ ] **Step 1: Write the vignette**

```markdown
---
title: "Propagating Tree Uncertainty"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Propagating Tree Uncertainty}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  eval = torch::torch_is_installed()
)
```

## Do I need this article?

**Short answer:** only if you have a **posterior sample of trees** (from BEAST, MrBayes, BirdTree.org, etc.) and you want the tree-topology uncertainty to show up in your pooled standard errors and p-values.

| Situation | Use this article? |
|---|---|
| One tree (published phylogeny, time-calibrated tree) | No — use [`multi_impute()`](../reference/multi_impute.html) and the [mixed-types vignette](mixed-types.html). |
| Posterior sample (≥ 2 trees) | Yes. |

## The two-step workflow

Tree uncertainty enters the analysis in two places. pigauto handles
step 1. Step 2 is your responsibility because the downstream model is
your choice.

```
┌─────────────────────────────────────┐
│ Step 1 — imputation                 │
│                                     │
│ multi_impute_trees(traits, trees)   │
│   -> T x m_per_tree completed       │
│      data.frames, each tagged with  │
│      the tree that produced it      │
└─────────────┬───────────────────────┘
              │
              v
┌─────────────────────────────────────┐
│ Step 2 — analysis + pool            │
│                                     │
│ for dataset i:                      │
│   fit model with trees[[t_i]]       │
│ pool_mi(fits)                       │
│                                     │
│ The SAME tree that produced         │
│ dataset i is used to fit model i.   │
└─────────────────────────────────────┘
```

## The canonical workflow (Nakagawa & de Villemereuil 2019)

With `share_gnn = TRUE` (the default), T = 50 posterior trees is cheap.
Use one imputation per tree (M = 50 total), fit the downstream model 50
times (each with the matching tree), and pool with Rubin's rules.

```{r canonical}
library(pigauto)
data(avonet300, trees300)
df <- avonet300
rownames(df) <- df$Species_Key
df$Species_Key <- NULL

mi <- multi_impute_trees(df, trees = trees300, m_per_tree = 1L)
# share_gnn = TRUE, reference_tree = MCC via phangorn -- all default

fits <- Map(
  function(dat, t_idx) {
    dat$species <- rownames(dat)
    nlme::gls(
      log(Mass) ~ log(Wing.Length),
      correlation = ape::corBrownian(phy = trees300[[t_idx]], form = ~species),
      data = dat, method = "ML"
    )
  },
  mi$datasets,
  mi$tree_index
)
pool_mi(fits)   # pooled SEs include both imputation and tree uncertainty
```

## Why `share_gnn = TRUE` is safe

The calibrated gate `r_cal` controls how much of each prediction comes
from the baseline vs the GNN. On every real dataset benchmarked in the
v0.9.0 validation suite (Delhey 5,809; AVONET 300; mixed-type
simulations), the gate closes or nearly closes under strong
phylogenetic signal. When the gate is closed, `pred = baseline(tree_t)`
— the GNN contributes nothing, and the per-tree baseline carries the
full tree-uncertainty signal. See `?multi_impute_trees` § Share-GNN
for the partially-open and fully-open cases.

If you need exact per-tree model independence (e.g. for methodological
comparison), set `share_gnn = FALSE`:

```{r opt_out, eval = FALSE}
mi_slow <- multi_impute_trees(df, trees300, m_per_tree = 1L,
                               share_gnn = FALSE)
# fits T = length(trees300) full pigauto models -- ~10-15x slower.
```

## Scale choices

| T | m_per_tree | M | When |
|---|---|---|---|
| 50 | 1 | 50 | Default. Canonical N&dV 2019. |
| 20 | 2 | 40 | Smaller posterior, still stable. |
| 10 | 5 | 50 | Very small posterior; per-tree variance helps. |
| <10 | bump m_per_tree | ≥ 25 | Runtime warning fires; Rubin's rules unstable below M=25. |

## References

- Nakagawa S, de Villemereuil P (2019). A general method for
  simultaneously accounting for phylogenetic and species sampling
  uncertainty via Rubin's rules in comparative analysis.
  *Systematic Biology* 68(4): 632-641.
- Rubin DB (1987). *Multiple Imputation for Nonresponse in Surveys*.
  Wiley.
- Jetz W et al. (2012). The global diversity of birds in space and
  time. *Nature* 491: 444-448.
```

- [ ] **Step 2: Build the vignette**

```bash
Rscript -e 'devtools::build_vignettes()'
```

Expected: produces `inst/doc/tree-uncertainty.html` (or similar, depending on setup). No errors.

- [ ] **Step 3: Add to `_pkgdown.yml` navbar**

Edit `_pkgdown.yml`. In the `articles:` menu (around the "Knitted vignettes" section), after the existing entry for "Mixed-type traits":

```yaml
        - text: "Propagating tree uncertainty"
          href: articles/tree-uncertainty.html
```

- [ ] **Step 4: Commit**

```bash
git add vignettes/tree-uncertainty.Rmd _pkgdown.yml
git commit -m "docs: new vignette 'Propagating Tree Uncertainty' + navbar entry"
```

---

## Task 9: R CMD check + full test suite

**Files:**
- (none modified; validation only)

- [ ] **Step 1: Run full test suite**

```bash
cd "/Users/z3437171/Dropbox/Github Local/pigauto"
Rscript -e 'devtools::test()'
```

Expected: all existing + new tests pass. No FAILs. Typical output: "Test passed 🟢" lines, final "[ FAIL 0 | WARN 0 | SKIP some | PASS many ]".

- [ ] **Step 2: Run R CMD check**

```bash
Rscript -e 'devtools::check()'
```

Expected: `0 errors ✓ | 0 warnings ✓ | 0 notes ✓` (or at most pre-existing notes — the diff should not INTRODUCE new notes). Typical NOTEs that are allowed to persist: "installed size", "non-standard files". Any new NOTE or WARNING related to our changes must be fixed before moving on.

- [ ] **Step 3: Run the share-gnn tests in isolation one more time**

```bash
Rscript -e 'devtools::test(filter = "share-gnn")'
```

Expected: all tests in `test-share-gnn.R` pass.

- [ ] **Step 4: Commit if any doc regeneration produced uncommitted changes**

```bash
git status -s
# If anything is listed (e.g. a stale .Rd), stage and commit:
git add man/*.Rd NAMESPACE
git commit -m "regenerate docs after share_gnn roxygen updates"
```

---

## Task 10: Push + open PR

**Files:**
- (none modified; git operations only)

- [ ] **Step 1: Push the branch**

```bash
git push -u origin feature/share-gnn-default
```

- [ ] **Step 2: Open the PR**

```bash
gh pr create --title "feat: share_gnn=TRUE default for multi_impute_trees" --body "$(cat <<'EOF'
## Summary

Makes `share_gnn = TRUE` the default in `multi_impute_trees()` and
flips `m_per_tree` default from 5 to 1 so the Nakagawa & de
Villemereuil (2019) canonical workflow (T=50 posterior trees, 1
imputation per tree, M=50 datasets pooled with Rubin's rules) is the
cheap default.

See `specs/2026-04-18-share-gnn-default-design.md` for the full design
rationale.

## What changed

- `R/predict_pigauto.R`: new `baseline_override` argument on
  `predict.pigauto_fit()`.
- `R/multi_impute_trees.R`: new `share_gnn` + `reference_tree`
  arguments; new internal helpers `resolve_reference_tree()` and
  `run_shared_gnn()`; `m_per_tree` default 5L → 1L.
- `R/multi_impute.R` and `multi_impute_trees.R` roxygen: new
  `@section When to use this:` decision guide; new `@section Share-GNN:`
  block covering tree-uncertainty propagation in gate-closed /
  partially-open / fully-open regimes.
- `DESCRIPTION`: `phangorn` added to Suggests (for
  `maxCladeCred()`).
- `tests/testthat/test-share-gnn.R`: new unit tests.
- `vignettes/tree-uncertainty.Rmd`: new pkgdown article.
- `_pkgdown.yml`: navbar entry for the new article.
- `NEWS.md`: v0.9.0.9000 entry.
- `script/bench_share_gnn.R`: smoke benchmark.

## Backward compatibility

- Existing `multi_impute_trees(df, trees, m_per_tree=5)` calls keep
  working and now run ~10× faster. Users who need exact per-tree
  independence set `share_gnn = FALSE` to restore pre-v0.9.1 behaviour.
- `multi_impute()` (single-tree MI) is unchanged.

## Test plan

- [x] `devtools::test()` — full suite green.
- [x] `devtools::check()` — no new errors/warnings/notes.
- [x] New tests in `tests/testthat/test-share-gnn.R` cover:
  predict baseline-override; `resolve_reference_tree` with phangorn;
  phangorn-missing fallback warning; `share_gnn = TRUE` return shape;
  `share_gnn = FALSE` return shape.
- [ ] Smoke benchmark (`script/bench_share_gnn.R`) shows the documented
  speedup ratio > 5× locally.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL printed. Record it.

---

## Self-review checklist

- [ ] **Spec coverage.** Each item in `specs/2026-04-18-share-gnn-default-design.md` maps to a task:
  - Decision 1 (`share_gnn = TRUE` default) → Task 4.
  - Decision 2 (MCC via phangorn + fallback) → Task 3.
  - Decision 3 (`m_per_tree` default flip + low-M warning) → Task 4.
  - Decision 4 (single-tree users unaffected) → Task 5 `@section When to use this:`.
  - Decision 5 (correctness / gate-state table) → Task 5 `@section Share-GNN:` + Task 8 vignette.
  - API surface (§ API surface in spec) → Task 4.
  - Implementation sketch → Tasks 2-4.
  - Testing (§ Testing in spec) → Tasks 2, 3, 4, 7, 9.
  - Documentation deliverables → Tasks 5, 6, 8.
  - Backward compatibility note → Task 6 (NEWS).
  - Success criteria → Task 9 (R CMD check + test suite) + Task 7 (smoke bench speedup).

- [ ] **Placeholder scan.** No "TBD", "implement later", or "similar to Task N" in any step. All `@param` lines, code blocks, and commands are concrete.

- [ ] **Type consistency.** `baseline_override = NULL` is used identically in Tasks 2, 4 (call site), and the plan's roxygen. `share_gnn`, `reference_tree` names match across all references. `resolve_reference_tree()` and `run_shared_gnn()` spelled identically everywhere.

---

## Execution handoff

Plan complete and saved to `plans/2026-04-18-share-gnn-default.md`.

Two execution options:

1. **Subagent-driven (recommended for this plan)** — I dispatch a fresh
   Sonnet subagent per task, I review each task's diff before the next
   dispatches, fast iteration. Good fit here because the tasks are
   bite-sized and independent.
2. **Inline execution** — I execute tasks in this session with
   checkpoints for review between logical groupings (Tasks 1-4,
   5-6, 7-8, 9-10).

Preferred?
