# pigauto (development version)

* Initial package scaffold. Implements `preprocess_traits()`, `read_tree()`, `read_traits()`, `mask_missing()`, and `make_missing_splits()` for data preparation. Includes `avonet300` and `tree300` as example datasets.
