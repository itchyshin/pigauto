% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validate.R
\name{cross_validate}
\alias{cross_validate}
\title{k-fold cross-validation for pigauto trait imputation}
\usage{
cross_validate(
  data,
  tree,
  k = 5L,
  seeds = NULL,
  epochs = 500L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{pigauto_data object (output of \code{\link{preprocess_traits}}).}

\item{tree}{phylo object.}

\item{k}{integer.  Number of folds (default 5).}

\item{seeds}{optional integer vector. Seeds for replicate runs. The default
\code{NULL} performs one run using the current RNG stream.}

\item{epochs}{integer.  Training epochs per fold (default 500).}

\item{verbose}{logical.  Print progress (default TRUE).}

\item{...}{Additional arguments passed to \code{\link{fit_pigauto}}
(e.g. \code{hidden_dim}, \code{k_eigen}, \code{use_attention}).}
}
\value{
A list of class \code{"pigauto_cv"} with:
\describe{
\item{results}{Data.frame with columns: fold, rep, trait, type,
metric, value.}
\item{summary}{Data.frame with mean and sd across folds/reps for
each trait + metric.}
\item{conformal_coverage}{Data.frame of coverage per trait across
folds (if available).}
\item{k}{Number of folds.}
\item{n_reps}{Number of replicates.}
}
}
\description{
Performs stratified k-fold cross-validation by rotating which cells
serve as the test set.  Returns per-fold and aggregated metrics.
}
\examples{
\donttest{
data(avonet300, tree300, package = "pigauto")
tree <- ape::keep.tip(tree300, tree300$tip.label[seq_len(30L)])
traits <- avonet300[match(tree$tip.label, avonet300$Species_Key),
                     c("Mass", "Wing.Length"), drop = FALSE]
rownames(traits) <- tree$tip.label
cv <- cross_validate(preprocess_traits(traits, tree), tree, k = 3L,
                     seeds = 1L, epochs = 5L, verbose = FALSE)
print(cv)
summary(cv)
}
}
