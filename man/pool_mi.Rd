% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool_mi.R
\name{pool_mi}
\alias{pool_mi}
\title{Pool downstream model fits across multiple imputations (Rubin's rules)}
\usage{
pool_mi(
  fits,
  conf.level = 0.95,
  coef_fun = NULL,
  vcov_fun = NULL,
  df_fun = NULL,
  tidy_fun = NULL
)
}
\arguments{
\item{fits}{A list of model fits of length \code{M >= 2}. Automatic fixed-effect
adapters are provided for \code{\link[stats:lm]{stats::lm}}, \code{\link[stats:glm]{stats::glm}}, \code{nlme::gls},
\code{nlme::lme}, \code{lme4::merMod}, \code{glmmTMB::glmmTMB}, \code{drmTMB}, and
\code{gllvmTMB_multi} fits. Other classes implementing \code{coef()} and \code{vcov()}
also work. The output of \code{\link[=with_imputations]{with_imputations()}} is accepted directly.
\code{MCMCglmm} fits are rejected -- see Details.
Bare user-supplied fit lists are accepted with an explicit warning because
their imputation provenance cannot be verified.}

\item{conf.level}{Confidence level for the pooled interval (default
\code{0.95}).}

\item{coef_fun}{Optional function extracting a named numeric fixed-effect
vector from one fit. \code{NULL} uses the automatic class adapter. Custom
coefficient and covariance extractors can be supplied independently;
callers must ensure custom extractors select fixed effects only.}

\item{vcov_fun}{Optional function extracting the fixed-effect covariance
matrix. \code{NULL} uses the automatic class adapter. Base matrices and
\code{Matrix} objects are accepted; the selected covariance block must be
square, finite, symmetric, and have non-negative diagonal entries.}

\item{df_fun}{Optional function returning the complete-data residual
degrees of freedom \code{nu_com} from one fit. When supplied, pooled
degrees of freedom use the Barnard & Rubin (1999) small-sample
correction, which is less biased for short series. When \code{NULL} (the
default) the classical Rubin (1987) formula is used.}

\item{tidy_fun}{Optional function returning a data.frame with unique
\code{term}, numeric \code{estimate}, and non-negative finite \code{std.error} columns.
This is an alternative to \code{coef_fun} and \code{vcov_fun}, not a supplement;
combining them is an error. Callers must ensure it selects fixed effects
only.}
}
\value{
A data.frame with one row per coefficient and columns:
\describe{
\item{\code{term}}{Coefficient name.}
\item{\code{estimate}}{Pooled point estimate (\code{mean} across fits).}
\item{\code{std.error}}{Pooled standard error \code{sqrt(T)} where
\code{T = W + (1 + 1/M) * B}.}
\item{\code{df}}{Pooled degrees of freedom (Barnard-Rubin if \code{df_fun}
supplied, else classical Rubin).}
\item{\code{statistic}}{\code{estimate / std.error}.}
\item{\code{p.value}}{Two-sided p-value from a t distribution on \code{df}.}
\item{\code{conf.low}, \code{conf.high}}{Pooled \code{conf.level} interval.}
\item{\code{fmi}}{Fraction of missing information.}
\item{\code{riv}}{Relative increase in variance due to non-response.}
}
}
\description{
Combine regression coefficients from \code{M} model fits -- one per imputed
dataset -- into a single pooled table using Rubin's rules. The pooled
standard errors combine within-imputation sampling variance and
between-imputation variance. Correct Rubin arithmetic does not make an
incompatible imputation model inferentially valid. For pigauto inference,
fits should come from the documented \code{\link[=multi_impute_analysis]{multi_impute_analysis()}} workflow.
The backend is experimental and supports fixed-effect coefficients only. Variance
components, correlations, random-effect predictions, BLUPs/conditional
modes, latent loadings, and other structured parameters are unsupported.
}
\details{
Let \eqn{\hat\theta_i} be the coefficient vector from fit \emph{i} and
\eqn{U_i = \mathrm{vcov}(\mathrm{fit}_i)}, for \eqn{i = 1, \ldots, M}.
Rubin's rules (Rubin 1987) give
\deqn{\bar\theta = M^{-1} \sum_i \hat\theta_i}
\deqn{W = M^{-1} \sum_i \mathrm{diag}(U_i)}
\deqn{B = (M-1)^{-1} \sum_i (\hat\theta_i - \bar\theta)^2}
\deqn{T = W + (1 + 1/M) B}
with pooled standard error \eqn{\sqrt{T}}. The relative increase in
variance is \eqn{r = (1 + 1/M) B / W}, the classical pooled df is
\eqn{\nu_{\text{old}} = (M-1)(1 + 1/r)^2}, and the fraction of missing
information is
\deqn{\mathrm{fmi} = (r + 2/(\nu + 3)) / (r + 1).}
When \code{df_fun} returns finite complete-data df \code{nu_com}, the
Barnard-Rubin (1999) correction combines
\eqn{\nu_{\text{obs}} = ((\nu_{\text{com}}+1)/(\nu_{\text{com}}+3))
\nu_{\text{com}} (1 - \lambda)} with \code{nu_old} via
\eqn{\nu_{\text{BR}} = 1/(1/\nu_{\text{old}} + 1/\nu_{\text{obs}})}.
With no between-imputation variation (\code{B = 0}), the classical limit is
\code{df = Inf}, \code{riv = 0}, and \code{fmi = 0}. If finite complete-data df are
supplied, Barnard--Rubin instead retains finite observed-data df and its
small-sample FMI adjustment. A completely deterministic quantity
(\code{B = W = 0}) always has zero FMI and infinite df.

Only fixed-effect coefficients are pooled in version 0.10.0. Random-effect
variances and correlations, BLUPs/conditional modes, latent loadings, and
other structured parameters require parameter-specific transformations and
are not supported by the automatic \code{pool_mi()} adapters. Custom extractors
are an expert escape hatch and cannot be inspected by pigauto; using them to
select unsupported structured parameters is outside the documented scope.
The \code{glmmTMB} adapter selects conditional fixed effects only. The \code{drmTMB}
adapter includes named distributional fixed-effect blocks such as regression
coefficients for \code{mu} and \code{sigma}; those are fixed coefficients, not
random-effect variance components.

\strong{MCMCglmm fits} are rejected because Rubin's rules are not the right
tool for posterior samples: variance decomposition does not generalise
cleanly to posterior distributions. No MCMCglmm downstream workflow is
supported by the initial analysis-aware backend.
}
\examples{
\donttest{
dat <- data.frame(y = stats::rnorm(30L), x = stats::rnorm(30L),
                  z = stats::rnorm(30L))
dat$x[seq(3L, 30L, by = 5L)] <- NA_real_
mi <- multi_impute_analysis(
  data = dat, formula = y ~ x + z, missing = "x",
  model = "lm", m = 2L
)
fits <- with_imputations(mi, function(d) lm(y ~ x + z, data = d))
pool_mi(fits)
}

}
\references{
Rubin DB (1987). \emph{Multiple Imputation for Nonresponse in Surveys.}
Wiley.

Barnard J, Rubin DB (1999). "Small-sample degrees of freedom with
multiple imputation." \emph{Biometrika} 86(4): 948-955.

Nakagawa S, Freckleton RP (2008). "Missing inaction: the dangers of
ignoring missing data." \emph{Trends in Ecology & Evolution} 23(11):
592-596.

Nakagawa S, Freckleton RP (2011). "Model averaging, missing data and
multiple imputation: a case study for behavioural ecology."
\emph{Behavioral Ecology and Sociobiology} 65(1): 103-116.
}
\seealso{
\code{\link[=multi_impute_analysis]{multi_impute_analysis()}}, \code{\link[=with_imputations]{with_imputations()}}
}
